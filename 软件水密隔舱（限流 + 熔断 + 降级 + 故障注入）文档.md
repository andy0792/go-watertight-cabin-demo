# 软件水密隔舱（限流 + 熔断 + 降级 + 故障注入）文档.md
> 项目名称：go‑watertight‑cabin‑demo
> Go实现网页可视化教学Demo，演示**水密隔舱Bulkhead故障隔离模式**，结合信号量并发限流、CircuitBreaker熔断器、业务Fallback降级、网页故障注入、**Canvas时序泳道图**。
> 类比轮船水密隔舱：单个舱故障进水，不会造成整艘船沉没；业务模块故障，不会把整个应用拖垮，实现故障隔离。
> 💡阅读提示：**先跑通上面实验，看懂网页现象之后，再来阅读本章节源码分析。**

## 💡分布式保护三板斧：限流、熔断、降级
> 💡分布式高可用保护三板斧：**限流、熔断、降级**

- **限流**：抵御突发大流量，控制并发/请求量，超出阈值直接拒绝请求，保护自身服务不被流量打垮；属于“防外部流量涌入”。
- **熔断**：感知下游依赖大量报错、超时，切断对故障下游的调用，避免无效调用堆积资源，防止故障链式扩散；属于“防下游故障传染”。熔断器具备 `Closed(正常)`‑`Open(熔断)`‑`HalfOpen(半开试探)` 状态流转。
- **降级**：当系统压力大或依赖故障时，主动舍弃非核心业务逻辑，返回兜底数据/简化逻辑，保障核心业务可用；属于“自身主动取舍保核心”。

三者配合分工：**限流挡流量，熔断断坏依赖，降级保核心业务**。本Demo完整实现限流、熔断、熔断触发后的fallback业务降级兜底；新增**结构化事件记录 + Canvas时序泳道可视化**，事件流图形化展示；同时支持网页端可视化故障注入，不用修改代码即可模拟下游各类故障工况。

> 💡容易混淆点：熔断是手段，降级往往是熔断触发之后执行的兜底行为。熔断器打开之后，一般就执行降级逻辑返回兜底结果。

#### 💡一句话区分
* **限流**：防流量太大，管入口，挡住突发流量，保护自己不被打垮。
* **熔断**：防雪崩，管下游，下游大量报错就切断调用，不再访问故障依赖。
* **降级**：防自己扛不住，管自身，舍弃非核心逻辑，返回兜底结果保核心可用。

> 💡记忆口诀：**限流挡流量，熔断断下游，降级保自己**。

#### 熔断器状态流转（文字版）
```
Closed正常（允许访问）
        ↓（错误占比超过阈值）
Open熔断阻断（拒绝访问）
        ↓（等待熔断冷却时间到期）
HalfOpen试探恢复（小流量测试）
    ├─→ 业务调用成功 → 回到 Closed正常（允许访问）
    └─→ 业务调用失败 → 回到 Open熔断阻断（拒绝访问）
```

文字描述：
1. **Closed（正常）**：正常接收业务请求，统计窗口内持续统计成功/失败；当失败占比达到设定阈值，切换到 Open。
2. **Open（熔断阻断）**：直接拒绝真实下游调用；营销舱命中降级兜底逻辑，返回缓存数据；其它舱直接返回熔断拒绝错误。
3. **等待冷却窗口期**：Open状态持续等待配置的冷却时间，到期自动进入 HalfOpen。
4. **HalfOpen（试探恢复）**：放行少量试探流量去访问真实下游服务。
   - 试探请求成功：熔断器判定下游已恢复，切回 Closed，恢复全部流量；
   - 试探请求失败：判定下游依旧故障，立刻切回 Open，继续阻断请求。

## 目录
1. [项目功能简介](#1‑项目功能简介)
2. [核心概念通俗讲解](#2‑核心概念通俗讲解)
3. [系统整体架构](#3‑系统整体架构)
4. [编译\&运行命令说明](#4‑编译运行命令说明)
5. [网页界面说明](#5‑网页界面说明)
6. [完整演示实验操作指南](#6‑完整演示实验操作指南)
7. [调参实验参考](#7‑调参实验参考)
8. [优缺点与适用场景](#8‑优缺点与适用场景)
9. [课堂演示话术（可直接复制讲解）](#9‑课堂演示话术可直接复制讲解)
10. [生产环境注意事项与坑点](#10‑生产环境注意事项与坑点)
11. [常见现象答疑](#11‑常见现象答疑)
12. [单元测试、接口测试、集成测试执行与命令说明](#12‑单元测试接口测试集成测试执行与命令说明)
13. [迭代记录 \& 后续可迭代开发方向](#13‑迭代记录--后续可迭代开发方向)
14. [关键代码说明](#17‑关键代码说明)

## 1‑项目功能简介
Demo分为**Go后端** + **HTML/JS前端**两部分。每个业务拥有独立隔离舱，舱与舱之间完全隔离互不影响。

### 后端能力（go‑go.go）
1. **信号量并发限流**：控制单个业务最大并发请求，超过上限直接快速拒绝，不堆积协程。
2. **熔断器故障熔断**：统计业务请求的成功、失败，失败占比达到阈值触发熔断，拒绝新请求，防止故障雪崩；支持半开试探配额控制，限制半开状态最大试探请求数量。
3. **熔断冷却+半开试探恢复**：熔断经过冷却时间，进入半开状态，放行请求探测业务是否已经恢复；试探成功恢复正常；试探失败立刻再次熔断。
4. **业务降级Fallback**：营销舱支持降级，熔断器Open时执行兜底函数返回缓存数据，不再调用真实业务；下单、报表舱无降级。
5. **内存故障注入模块**：营销舱、报表支持网页端配置故障开关、故障百分比；无需修改代码、无需重新编译，模拟下游正常、不稳定、完全故障。核心舱‑下单不支持故障注入，业务始终正常。
6. **HTTP接口服务**：提供全套REST接口，供前端调用；同时提供静态文件服务托管前端页面。
7. **结构化时序事件模块**：后端记录结构化事件（时间戳、所属舱、事件类型、消息文本），限制最大200条防止内存暴涨；接口输出事件数组给到前端。
8. **指标统计**：每个舱独立统计总任务、成功、限流拒绝、熔断拒绝、业务报错、降级次数；同时输出熔断器窗口内部成功、失败计数。
9. **简易协程错误组SimpleErrGroup**，用于批量发起模拟请求，支持上下文取消。

### 前端能力（index.html）
> 前端**不实现任何限流熔断业务逻辑**，只负责界面渲染、用户交互、调用后端接口、Canvas时序泳道绘图，全部状态数据来源于后端接口。
1. **舱卡片UI**：3张独立业务卡片，卡片底色跟随熔断器状态变色；投屏远距离可以直观识别故障舱。
2. **指标表单**：展示全部业务指标、熔断器窗口统计；每个舱可在线修改全部配置参数，提交后端生效。
3. **故障注入UI**：可视化配置故障开关、故障百分比。
4. **一键场景按钮**：场景A/B/C，一键完成参数+故障配置，课堂演示省去手动填参。
5. **流量操作按钮组**：批量请求、等待窗口期、试探请求、两套重置逻辑；任务运行期间按钮自动置灰，防止重复点击导致指标错乱。
6. **Canvas时序泳道可视化**
    - 每个业务舱一行泳道，不同颜色区分事件类型；X轴自适应相对毫秒时间刻度。
    - **智能堆叠**：同一时间点2‑6个事件自动排列2行矩阵，球体清晰分开。
    - **溢出聚合**：超过6个事件显示`+N`聚合气泡，鼠标悬浮查看明细。
    - 鼠标悬浮圆点/气泡查看事件完整详情。
7. **文本日志面板+过滤下拉**：支持多种事件过滤模式；**过滤同时联动文本日志和Canvas时序泳道图**。
8. **800ms轮询更新**，输入框焦点保护：用户编辑输入框时，轮询不会覆盖正在编辑的内容。

> 内置3个业务舱分工
* **核心舱‑下单**：演示**限流功能**。模拟核心交易，无降级；失败就返回错误，保证数据真实；**无故障注入开关，下游永远正常**。
* **营销舱**：演示**降级Fallback**功能，模拟普通业务，当熔断器判定下游异常时，执行降级逻辑，返回兜底数据，不向上层抛 error；**支持故障注入开关、故障百分比配置**。
* **辅助舱‑报表**：演示**熔断功能**，模拟容易故障的下游服务（慢 SQL，大量报错），用来演示熔断、故障隔离；**支持故障注入开关、故障百分比配置，无降级兜底**。

## 2‑核心概念通俗讲解
|概念|通俗比喻|网页UI标识|
|-|-|-|
|水密隔舱 Bulkhead|轮船独立隔间，一个隔间故障，其他隔间不受影响|3张独立业务卡片面板|
|并发限流（信号量）|窗口最多同时接待N个人，人多直接告知繁忙，不全部堵在门口|参数：最大并发；时序橙色`reject`圆点|
|熔断器 Closed|业务健康，全部请求放行|🟢 Closed正常（允许访问），卡片浅绿色底色|
|熔断器 Open|业务故障，拒绝新来请求，阻止故障扩散|🔴 Open熔断阻断（拒绝访问），卡片浅红色底色|
|熔断器 HalfOpen|冷却结束，放少量请求试探业务是否修复|🟡 HalfOpen试探恢复（小流量测试），卡片浅黄色底色|
|降级Fallback|熔断打开，不走真实业务，返回缓存兜底数据|指标：降级次数；时序灰色`fallback`圆点|
|故障注入|网页开关模拟下游故障概率，0‑100%可调|每个舱面板：模拟业务故障勾选框 + 故障%输入框|
|故障隔离|报表业务故障熔断，下单、营销业务依旧正常工作，不会被拖垮|三个舱状态互不干扰，卡片底色独立变化|
|时序事件泳道图|将所有保护事件按时间、所属舱图形化展示|页面中部「时序事件可视化」Canvas画布，带图例|

> ⚠重要区分：
>
> **限流拒绝 ≠ 业务报错 ≠ 降级**
>
> - 限流拒绝：并发已满，请求被挡在舱入口，业务逻辑完全没有执行，不计入熔断器失败统计。
> - 业务报错：请求成功进入舱并执行业务，代码抛出错误，这个才会参与熔断器失败率计算。
> - 降级：熔断器Open，执行fallback兜底函数，**完全不执行真实业务**，返回成功兜底结果。

时序图颜色映射表
| 事件类型 | 颜色 | 含义 |
|---|---|---|
| normal | 绿色`#67c23a` | 正常事件、业务成功 |
| reject | 橙色`#e6a23c` | 限流拒绝事件 |
| open | 红色`#f56c6c` | 熔断器打开熔断 |
| halfOpen | 蓝色`#409eff` | 半开状态事件 |
| fallback | 灰色`#909399` | 降级兜底触发 |
| error | 红色`#ff4d4f` | 业务执行报错 |
| config | 紫色`#9b59b6` | 配置变更、故障设置事件 |

## 3‑系统整体架构
```
浏览器网页前端(index.html)
        ↓ HTTP fetch接口
Go后端服务(go‑go.go)
├‑3个 BusinessCabin 水密业务舱（互相完全隔离）
│  ├‑核心舱‑下单（无降级、无故障注入）
│  ├‑营销舱（支持 fallback降级 + 网页故障注入）
│  └‑辅助舱‑报表（无降级 + 网页故障注入）
│
├‑CircuitBreaker熔断器：Closed / Open / HalfOpen三状态机；半开试探配额控制
├‑semaphore信号量：每个舱独立控制最大并发
├‑SimpleErrGroup：协程组，批量发起模拟请求
├‑全局faultConfig故障配置：保存各舱故障开关、故障百分比（内存存储，重启丢失）
├‑Metrics指标：每个舱独立统计：总任务、成功、限流拒绝、熔断拒绝、业务报错、降级次数DegradeCount
└‑时序事件模块
    ├‑TimelineEvent 结构体：ts毫秒时间戳、Cabin舱名、EvType事件类型、Message消息
    ├‑logTimelineEvent()：同时写入普通文本日志 + 结构化时序事件；最多保留200条自动截断防内存暴涨
    └‑/api/status接口返回events数组给到前端渲染Canvas泳道图

Go静态文件服务：托管 index.html 前端页面
```

### HTTP接口总览
|接口|请求方式|作用|
|-|-|-|
|`/api/status`|GET|获取全部舱状态、指标、日志、故障配置、events时序事件数组，前端800ms定时拉取刷新页面|
|`/api/setFault`|POST|设置故障注入：舱名称、故障开关enable、故障百分比faultPercent|
|`/api/runFirst`|POST|发起第一轮批量模拟请求|
|`/api/waitHalfOpen`|POST|等待熔断恢复窗口期sleep|
|`/api/runSecond`|POST|发起第二轮试探请求|
|`/api/reset`|POST|重置全部舱，指标清零、日志清空、清空时序事件，恢复初始状态|
|`/api/applyConfig`|POST|接收网页参数，重建业务舱，动态更新配置（重建舱需要手动复制fallback回调函数）|
|`/api/clearLogs`|POST|仅清空事件日志与时序事件，**不修改舱指标、熔断器状态**|
|`/api/resetMetricsOnly`|POST|仅清空业务指标与文本日志；保留熔断器状态、窗口统计、时序事件历史|

> 前端渲染优化说明：
> - 仅舱卡片数量变化才全量重建DOM；平时只局部更新指标、熔断器状态文本；
> - 使用`document.activeElement`焦点判断，**光标在输入框编辑时，不会被轮询刷掉用户输入**。
> - Canvas时序泳道图：每次轮询拿到后端events，调用`renderTimelineSvg()`重绘泳道图；重置/清空日志同步清空前端内存`eventList`。

## 4‑编译&运行命令说明
> 🎯非开发人员优先阅读本节：拿到源码第一步，先把项目跑起来，先看效果再研究代码。

项目文件清单：
1. `go‑go.go` Go后端源码
2. `index.html` 前端页面，必须和go源码放在同一个目录
3. `go.mod`、`go.sum` go模块依赖文件

> 只拷贝`.go`源码，不带 mod 文件，会报 `no required module provides package golang.org/x/sync/semaphore`。

Windows CMD完整操作：
```cmd
# 进入工程目录
cd /d D:\\go‑watertight‑cabin

# 1.初始化go模块（已有go.mod可跳过）
go mod init go‑watertight‑cabin
# 配置国内代理，解决依赖拉取网络失败
go env -w GOPROXY=https://goproxy.cn,direct
# 2.自动扫描代码，下载补齐全部第三方依赖
go mod tidy

# 源码方式运行项目
go run go‑go.go
```

浏览器访问：`http://127.0.0.1:8080`，建议无痕窗口，避免浏览器缓存干扰页面渲染。

```cmd
# 编译生成 exe 可执行程序
go build .
```

### 单元测试全套命令（开发调试使用）
> ⚠本项目存在大量全局共享变量，测试必须串行执行 `-p 1`，避免用例之间状态互相污染。
```bash
# 基础单元测试，串行执行
go test -v -p 1
# 带超时、覆盖率执行测试
go test -v -p 1 -cover -timeout 20s
# 生成覆盖率统计文件 cover.out
go test -v -p 1 -coverprofile=cover.out
# 浏览器打开可视化覆盖率报告
go tool cover -html=cover.out
```

## 5‑网页界面说明
页面从上往下布局顺序：
1. 页面标题、项目说明文本
2. 📋①选择演示场景：3个一键场景按钮（场景A/B/C）
3. ⚙️②执行流量操作：批量请求、等待窗口期、试探请求、重置按钮组；附带文字提示区分两套重置逻辑
4. **舱状态卡片容器 `#container`**（3个业务舱卡片，卡片底色跟随熔断器状态变色）
5. **时序事件可视化区块**
   * h4标题：时序事件可视化
   * 颜色图例说明
   * Canvas泳道画布（X轴相对时间、各舱泳道、彩色事件圆点，鼠标悬浮看详情）
6. 操作栏：【清空日志】红色按钮 + 日志过滤下拉框（文本日志与Canvas时序泳道图同步过滤）
7. 黑色日志输出面板 `#logPanel`
8. `#batchTip`批量请求计数提示
9. 页脚

### 舱卡片内部
每个业务舱独立卡片：
1. 舱名称 + 熔断器状态标签
2. metric‑grid指标网格
    - 总任务：进入本舱的全部请求数量
    - 成功：业务逻辑正常返回成功
    - 限流拒绝：并发已满，被信号量拦截（业务未执行）
    - 熔断拒绝：熔断器打开，无降级时拒绝请求
    - 业务报错：请求已经进入舱执行业务抛出异常
    - 降级次数：熔断器Open，执行fallback兜底的请求计数
    - 窗口统计：**窗口成功:N 失败:N 当前失败占比:XX%**
    > ✨熔断触发必须**两个条件同时满足**：
    > 1. 窗口总请求数（成功+失败）≥【统计样本请求数】
    > 2. 当前失败占比 ≥【失败触发熔断%】
3. 参数输入框（每个业务舱独立配置）

|参数|说明|
|-|-|
|最大并发|该业务舱允许同时处理的请求上限，超过直接限流拒绝|
|失败触发熔断%|统计样本内，失败请求占比达到该百分比就触发熔断|
|统计样本请求数|需要收集多少条请求作为判断样本；样本数量不足不会触发熔断|
|熔断冷却时间(s)|熔断打开之后，等待多少秒，才进入半开试探阶段|
|半开试探最大请求数|HalfOpen状态下允许放行的试探请求配额，配额耗尽直接拒绝|
|正常耗时(ms)|业务成功后模拟延时|
|故障耗时(ms)|业务报错后模拟延时|

> 修改参数之后，点击蓝色【应用配置】，参数才提交后端生效。

4. 故障注入面板（营销舱、报表辅助舱可见）
* `模拟业务故障`勾选框：故障总开关，勾选才会启用故障百分比；
* `故障%`：0‑100，100代表每次请求必模拟业务报错；
* 【设置故障】按钮：修改完成必须点击按钮，配置才提交后端生效。

### 舱卡片状态背景色
* 🟢 Closed正常：卡片**浅绿色底色**
* 🟡 HalfOpen试探恢复：卡片**浅黄色底色**
* 🔴 Open熔断阻断：卡片**浅红色底色**
> 投屏演示时，无需细读文字，通过卡片底色即可一眼识别哪个舱发生熔断，直观体现水密隔舱故障隔离效果。

### Canvas时序泳道图特性
1. **智能堆叠**：同一时间窗口内2‑6个事件自动排布为2行矩阵，上排最多3，下排最多3；每个圆点白色描边，视觉分离。
2. **溢出聚合**：同一时刻事件数量>6，显示`+N`聚合气泡；鼠标悬浮气泡展示各事件类型统计明细。
3. **自适应X轴时间刻度**，自动切换ms/s/m时间单位。
4. **过滤联动**：下拉选择过滤类型，画布与文本日志同时过滤。

### 按钮完整功能
|按钮|功能|
|-|-|
|场景A‑限流挡流量|一键配置下单舱限流参数，关闭故障；限流演示|
|场景B‑降级保自己（熔断+降级）|营销舱长冷却时间30s，开启100%故障；演示降级兜底|
|场景C‑熔断防雪崩|报表舱开启故障注入，演示完整熔断器生命周期与故障隔离|
|① 发起第一轮批量请求|发送批量模拟请求，制造业务流量积累样本触发熔断|
|② 等待熔断恢复窗口期|后端sleep1.2s，等待冷却，观察是否切换HalfOpen半开状态|
|③ 发起第二轮试探请求|半开状态下发请求探测下游是否恢复，仅报表舱发送请求|
|④ 重置所有舱状态|完整重置：指标清零、熔断器切Closed；清空文本日志+清空时序事件|
|仅重置指标(保留熔断器状态)|只清空业务metrics指标、文本日志；**保留熔断器状态、窗口统计、时序事件历史**|
|清空日志|仅清空文本日志与时序事件画布；舱配置、指标、熔断器状态完全不动|

> ⚠演示降级小技巧：营销舱熔断冷却时间改成30秒；打到Open红色熔断后，**禁止点重置**，在冷却窗口期内再次发请求，观察降级次数上涨；超过冷却时间后端自动切HalfOpen，降级不会触发。

## 6‑完整演示实验操作指南
> 💡课堂演示优先使用紫色一键场景按钮，省去手动填写参数，节省演示时间；需要讲解参数含义时，使用手动调参流程。
> 💡观察提示：做熔断相关实验，除了看状态文字，同时留意**舱卡片底色、Canvas时序泳道事件、窗口统计行数值**。
> 💡投屏演示小技巧：演示熔断器状态流转，可以将日志过滤切换为【仅熔断器事件】；演示降级现象切换【仅降级事件】；实验开始前点【清空日志】，排除上一轮残留日志干扰。

### 🧪实验一：演示【并发限流】
> 目标：观察并发超限出现限流拒绝，**熔断器保持绿色，不会触发熔断**。

✅ **方式1：一键场景快速操作（推荐课堂演示）**
1. 点击紫色按钮【场景 A‑限流挡流量】，程序自动完成重置，设置核心舱‑下单最大并发 = 2，关闭全部故障注入。
2. 点击 ① 发起第一轮批量请求。

**观察现象：**
* 下单舱状态依旧 🟢 绿色正常；Canvas时序泳道图出现橙色`reject`限流拒绝圆点；
* 限流拒绝计数上涨，成功等于设置的最大并发；
* **原理：** 并发已满直接拒绝，业务没有执行，不会产生业务报错，熔断器不会触发熔断。

✅ **方式 2：手动调参操作**
1. 点击【④重置所有舱状态】清零；
2. 修改【核心舱‑下单】参数：最大并发=2；其余参数保持默认；点击【应用配置】；确认营销舱、报表舱故障全部关闭。
3. 点击`① 发起第一轮批量请求`。
4. 观察现象：状态🟢绿色，限流拒绝计数上涨。

### 🧪实验二：完整演示熔断器生命周期（重点）
> 使用【辅助舱‑报表】，开启故障注入，故障%=100，模拟下游完全故障。

✅ **方式1：一键场景快速操作（推荐课堂演示）**
1. 点击紫色按钮【场景 C‑熔断防雪崩】，自动重置，报表舱配置完成，报表故障 100% 开启，营销舱故障关闭。
2. 点击 ① 发起第一轮批量请求。

**现象：**
业务报错持续上涨；样本收集完成，失败占比超过阈值，状态切换 🔴 红色 Open 熔断阻断；时序泳道出现红色`open`熔断事件。
3. 点击 ② 等待熔断恢复窗口期，冷却时间结束，状态变为 🟡 黄色 HalfOpen半开状态，时序出现蓝色`halfOpen`事件。
4. 点击 ③ 发起第二轮试探请求：
   * **分支 A（故障保持开启）：** 试探请求出现报错 → 立刻切回 🔴 红色熔断；
   * **分支 B（关闭故障）：** 在界面报表舱取消勾选模拟业务故障，点击设置故障；再执行③，试探全部成功 → 切回 🟢 绿色正常。

> ✨ **核心演示现象：** 报表舱故障熔断，下单舱、营销舱完全不受影响，实现故障隔离（水密隔舱核心思想）；Canvas时序泳道可以直观看到整套事件时间顺序。

### 🧪实验三：演示【营销舱熔断+降级Fallback】
> 前提：营销舱熔断冷却时间设置为 **30秒**，拉长窗口期方便演示降级。

✅ **方式1：一键场景快速操作（推荐课堂演示）**
1. 点击紫色按钮【场景 B‑降级保自己（熔断+降级）】；自动重置，营销舱冷却 30s，故障 100% 开启，报表舱故障关闭。
2. 反复点击 ① 发起第一轮批量请求，直到营销舱状态变为 🔴 红色 Open 熔断阻断；时序图出现红色open事件。
3. ⚠️ **必须在 30 秒冷却窗口期以内，立刻再次点击【① 发起第一轮批量请求】**

**预期现象：**
* 网页营销舱面板：**降级次数数字上涨，熔断拒绝(CbReject)保持0**；时序泳道出现灰色`fallback`降级事件；
* 不会执行真实`bizMarketing`业务函数，不会打印业务SUCCESS/DB超时报错。

> ⚠坑：如果等待超过30秒再点击，后端内部自动切换HalfOpen放行，降级不会触发。

## 7‑调参实验参考
修改参数，点击【应用配置】，再跑完整流程，观察现象。
1. **调高失败触发熔断%=80**：需要80%请求失败才熔断；同样大量报错，失败率只有66%，不会触发红色熔断。
2. **调高统计样本请求数=100**：需要攒够100条请求样本；只发送25条请求，样本不足，无论报错多少，不会熔断。
3. **调高熔断冷却时间(s)=30**：熔断变红后，需要等待30秒才进入半开状态；**降级演示必须调大这个参数**。
4. **调大最大并发=20**：限流拒绝变少，更多请求可以执行业务，更容易积累报错触发熔断。
5. **半开试探最大请求数=1**：半开状态仅仅放行1个试探请求，其余直接拒绝；模拟生产环境防止刚恢复下游被大流量打垮。
6. **正常耗时ms /故障耗时ms**：模拟业务RTT；故障耗时调大可以模拟大量慢请求耗尽并发。
7. **故障注入调参**
   * enable=true，faultPercent=100：每次必报错，快速打熔断；
   * enable=true，faultPercent=50：一半成功一半失败，可以观察熔断器震荡；
   * enable=true，faultPercent=0：开关打开但是业务全部成功，模拟下游故障恢复。

> 关键理解：熔断触发需要**两个条件同时满足**：①收集足够统计样本；②失败占比达到阈值。

## 8‑优缺点与适用场景
### ✅优点
1. **故障隔离，阻止雪崩（核心价值）**：单个业务故障，只会消耗本舱的并发配额，不会抢占其他业务资源；局部故障不会演变为整个服务雪崩。
2. **熔断配合降级，保留部分业务可用**：非核心业务故障熔断，执行fallback返回兜底缓存数据，保障接口不直接报错。
3. **快速失败，避免请求堆积**：信号量满直接拒绝；熔断打开优先执行降级/快速返回错误，减少无效等待、goroutine堆积。
4. **每个舱独立配置策略**：核心业务、非核心业务可以设置不同的并发、熔断阈值、降级回调。
5. **强可观测教学属性**：每个舱独立统计指标；Canvas时序泳道图图形化复现事件流；网页色彩化状态，投屏演示友好。
6. **网页故障注入**：无需修改代码即可模拟下游多种故障工况，教学演示非常方便。
7. **前端防护机制**：一键场景、运行按钮锁、输入框焦点保护、时序事件堆叠聚合，降低演示翻车概率。

### ⚠缺点与坑
1. **资源碎片化**：舱划分越多，资源越碎片化；空闲舱的并发配额，不能借给流量突增的其他舱使用，资源利用率下降。
2. **参数调优难度高**：参数设置错误会出现误限流、频繁熔断器震荡（Open ↔ HalfOpen来回跳动），需要压测结合业务P99延迟调参。
3. **仅进程内防护**：只能防护下游慢调用耗尽本进程goroutine、连接；**无法防护CPU打满、OOM、机器宕机**，不能替代K8s容器资源限制。
4. **Demo把所有error都统计为失败**；生产中用户参数错误、鉴权失败属于业务逻辑错误，**不应该计入熔断统计**，否则会误触发熔断。
5. **重建BusinessCabin，fallback降级回调容易丢失**；NewCabin会把fallback置nil，重建舱必须手动复制fallback回调。
6. **全部配置、事件、指标内存存储**：进程重启全部丢失；时序事件最多保留200条，仅教学演示，禁止直接上生产。
7. **前端是状态快照**：前端800ms轮询拿到后端状态快照；`cb.Allow()`后端内部会变更状态，页面UI显示状态和后端真实瞬时状态存在微小延迟。

### 🎯适合场景
1. 教学培训：讲解分布式限流、熔断、降级、故障隔离原理；课堂演示、学生实验。
2. 微服务学习，调用多个外部下游RPC、DB、第三方HTTP接口，下游存在慢、超时风险。
3. 系统区分核心业务、非核心业务，希望非核心故障不影响核心链路，熔断后执行本地降级兜底。
4. 存在报表、导出等重风险慢业务；希望防止慢调用耗尽goroutine、数据库连接池，避免服务雪崩。

### ❌不建议使用场景
1. 简单单体小项目，外部依赖很少，属于过度设计。
2. 故障根源是CPU/OOM进程整体资源耗尽，本模式无法解决。
3. 以消息队列异步消费为主的系统，隔离逻辑更多放在消费者侧。
4. 直接拿来作为生产运维页面或者生产服务组件。

## 9‑课堂演示话术（可直接复制讲解）
> **开场介绍**
“今天演示分布式系统两个重要保护机制：并发限流和熔断器，这个模型叫做水密隔舱，来自轮船设计。轮船划分多个隔间，一个隔间漏水，不会整艘船沉没。对应软件系统，某个业务模块故障，不能把整个系统拖垮。

我们这里有三个舱：核心舱‑下单、营销舱、报表辅助舱。报表、营销舱可以在网页打开模拟业务故障，模拟下游服务不稳定或者完全挂掉。营销舱额外实现了熔断后的降级兜底。

界面状态：🟢绿色正常放行，🟡黄色小流量试探，🔴红色熔断拒绝。中间时序事件泳道图，不同颜色圆点记录发生的各类保护事件，可以直观看到事件发生的先后顺序。”

> **演示一：并发限流**
“现在我们把下单舱最大并发设置为2，一次性发起一批请求。

大家可以看到限流拒绝计数上涨，时序图出现橙色限流事件，但是状态依旧是绿色。

👉重点：限流不等于业务报错。请求太多，直接挡在门外，业务代码根本没有跑，熔断器不会认为服务故障。限流用来保护自己，防止流量过大把服务压垮。”

> **演示二：熔断器完整生命周期（重点）**
“限流是防止被压垮；熔断器解决下游故障雪崩。

如果下游大量报错，我们还不停调用它，会出现大量超时，请求堆积，故障扩散到整个系统。熔断器就会断开，停止调用故障服务，给下游留出修复时间。

我们给报表舱开启模拟故障100%报错，大量业务报错，样本收集够，失败比例超过阈值，变成红色熔断阻断。时序图出现红色open事件，新的请求不再执行业务，直接拒绝。

熔断不会永远红色，等待冷却时间结束，进入黄色半开试探状态，放少量请求探测服务有没有修好。

如果试探依旧报错，说明服务还没修好，立刻再次熔断；我们取消模拟故障，下游恢复，试探全部成功，就恢复绿色正常。

大家注意：报表舱故障熔断，下单、营销舱完全不受影响。这就是水密隔舱故障隔离的价值。”

> **演示三：熔断+降级（营销舱）**
“熔断之后有两种处理策略：直接返回错误，或者执行降级兜底。

营销舱开启模拟故障打到熔断Open，熔断器Open之后，不再调用真实业务，直接执行本地缓存兜底逻辑，返回可用结果，不对外抛错误。时序图出现灰色fallback降级事件。

⚠️演示注意：要在熔断冷却窗口期之内发送请求；时间过期熔断器悄悄进入半开，就不会触发降级。”

> **总结**
“1.限流：保护自己，防止并发太大压垮服务；限流拒绝不等于业务失败。
2.熔断器：保护不被下游故障雪崩，只统计真正执行业务的成功失败。
3.降级：熔断打开之后的兜底策略，返回缓存数据，保障接口可用。
4.熔断器三个状态流转：正常 → 熔断阻断 → 半开试探。时序泳道图可以完整看到整套事件时间顺序。
5.不是一报错就熔断，要收集足够样本，失败占比达到阈值两个条件。
6.半开试探：成功就恢复，遇到失败立刻重新熔断。
7.网页故障注入可以快速模拟下游各种故障，不用修改代码。

注意这个是演示Demo，生产环境会更加复杂。”

## 10‑生产环境注意事项与坑点
> ⚠重要：本项目是教学Demo，**禁止直接复制上线生产环境**。

### 后端注意事项
1. **信号量限流**
Demo使用`TryAcquire`非阻塞快速拒绝；生产根据业务选择非阻塞快速失败或者阻塞等待；优先推荐非阻塞防止goroutine堆积。
2. **熔断器实现差异**
* Demo是简单计数窗口；工业级组件（Sentinel、Resilience4j）使用滑动窗口统计；
* Demo半开使用配额控制；生产半开状态只允许极少量试探请求，防止把刚恢复的下游再次打垮；
* **错误分类过滤：用户参数错误、鉴权失败不计入熔断失败统计，只统计下游依赖故障类错误**。
* `Allow()`不是只读查询，具备状态变更副作用；UI界面状态快照不等于真实运行时状态。
3. **降级Fallback回调**
* 重建业务舱对象时，手动复制fallback回调函数，防止降级逻辑悄悄丢失；
* fallback内部需要增加超时、panic保护，防止兜底逻辑本身异常。
4. **指标与监控告警**
Demo全部指标内存存储，重启丢失；生产需要上报Prometheus；熔断器状态切换、降级次数突增需要配置告警。
5. **舱粒度设计**
最佳实践：**按外部依赖划分舱，不要按接口划分舱**；一个外部下游对应一个舱，粒度不能过细，也不能过粗。
6. **防护组合拳**
完整的弹性防护顺序：**超时设置 > 舱壁隔离（信号量限流） > 熔断器 > 降级兜底返回数据**，多层防护缺一不可。
> 水密隔舱只是止损手段，**不会修复下游故障**；熔断发生之后，依旧需要人工排查修复故障服务，只是阻止故障扩散。

### 前端注意事项
1. **快照不等于真实状态**：前端800ms轮询拿到后端状态快照；后端`cb.Allow()`调用会发生状态变更，页面显示状态和后端瞬时状态可能存在微小延迟。生产监控需要事件推送，不要只靠轮询快照。
2. **按钮锁只是前端锁**：`isRunning`仅前端层面防止重复点击；可以绕过浏览器直接调用HTTP接口；生产后端接口必须增加幂等、重复调用防护。
3. **永远不信任前端入参**，后端接口必须做参数校验。
4. 时序事件内存存储只适合教学；生产时序事件不能内存存储，需要接入日志系统、时序数据库。
5. 本Demo无鉴权；生产环境必须增加身份认证。
6. 故障注入：Demo网页故障注入仅教学；生产环境故障注入请使用 Chaos‑Mesh 等专业混沌工程平台，不要自己实现。

## 11‑常见现象答疑
|问题|解答|
|-|-|
|**限流拒绝数字涨很多，熔断器依旧是绿色？**|限流拒绝代表请求被拦在入口，业务没有执行，不会算作失败，熔断器不会触发。时序泳道出现橙色reject事件。|
|**已经看到大量业务报错，熔断器不会变红？**|检查【统计样本请求数】，需要收集足够样本数量，才会计算失败占比；同时确认故障注入开关已经勾选。|
|**半开状态一发起第二轮请求，立刻再次熔断？**|辅助舱‑报表故障注入仍然开启，请求会报错；半开试探遇到报错，规则就是立刻切回熔断。时序图可以看到半开→open事件。|
|**网页UI显示Open熔断阻断，但是新请求没有触发降级？**|高频坑！`cb.Allow()`有状态变更副作用；熔断冷却时间到期，调用Allow会自动切换HalfOpen放行请求；**UI只是快照，不等于真实运行状态**。做演示调大熔断冷却时间，在窗口期之内发送请求。|
|**单元测试降级用例PASS，网页演示降级不生效？**|重建`BusinessCabin`结构体，忘记复制fallback回调函数，fallback被置nil；单元测试自己NewCabin并手动赋值fallback，不受影响。|
|**长时间反复跑演示，内存占用越来越高？**|Demo时序事件最大限制200条，日志最多80条；测试完成建议点击重置或者重启程序释放内存。|
|**熔断器已经Open，窗口统计数字不再上涨？**|Open熔断状态下请求直接被熔断器拦截，不会执行业务，因此不会计入窗口成功/失败统计，属于正常现象。|
|**执行请求时部分按钮变成灰色无法点击？**|正在执行后台任务，开启了按钮置灰保护，防止重复点击；任务执行完毕按钮自动恢复可用；重置按钮始终可以点击。|
|**点击【清空日志】之后，舱的状态、数字指标没有清零？**|**属于正常设计**。清空日志只清理日志面板与时序事件画布，不触碰业务舱指标、熔断器状态；想要指标全部清零请使用【④重置所有舱状态】按钮。|
|**时序泳道图空白看不到事件？**|没有产生业务事件；确认业务代码分支调用了`logTimelineEvent()`；仅仅调用`onEventLog()`只会出现在文本日志，不会出现在Canvas时序泳道图。|
|**时序图上为什么会出现 `+N` 的气泡？**|这是**智能聚合功能**。当同一时间点的事件超过6个时，为了防止圆点重叠遮挡，系统自动将它们合并显示为一个气泡，数字代表被合并的事件总数。鼠标悬浮可查看各类型事件明细。|
|**时序图上事件圆点为什么分上下两排？**|这是**智能堆叠功能**。同一时间点有2‑6个事件时，自动排列为2行矩阵（上排最多3个、下排最多3个），球体之间有明显间隙，视觉上清晰不粘连。|
|**重置指标按钮之后，时序事件图还保留历史事件？**|预期行为：「仅重置指标」只清空metrics、清空文本日志；**保留熔断器状态与时序事件历史**，方便完整观察熔断生命周期。|
|**修改输入框参数马上被页面轮询刷掉？**|光标停留在输入框内有焦点时不会被覆盖；失去焦点后同步后端配置。|
|切换过滤下拉，文本日志变，时序图也跟着过滤？|本Demo实现过滤联动，下拉选择会同时过滤文本日志与Canvas泳道图。|
|点击按钮页面没有变化？|检查Go后端是否正常运行；浏览器F12看网络请求；建议无痕窗口关闭缓存。|

## 12‑单元测试、接口测试、集成测试执行与命令说明
> 测试文件命名：`go‑go\_test.go`，必须以`\_test.go`结尾，和源代码放在同一个目录。
```bash
# 完整执行全部用例，-p 1串行执行；本项目存在全局共享舱、全局故障配置，必须串行，避免测试互相污染状态。
go test -v -p 1
# 带超时、覆盖率执行测试
go test -v -p 1 -cover -timeout 20s
# 只运行降级相关的测试用例（正则匹配TestMarketing开头）
go test -v -p 1 -run="TestMarketing"
# 生成覆盖率统计文件 cover.out
go test -v -p 1 -coverprofile=cover.out
# 浏览器可视化打开覆盖率报告
go tool cover -html=cover.out
```

### 新增测试覆盖范围（适配时序事件功能）
1. `TestLogTimelineEvent`：测试结构化事件写入、超过200条自动截断逻辑；
2. `TestApiStatus`接口测试：校验接口返回`events`数组；
3. `TestApiReset`：校验`/api/reset`会清空后端`globalTimelineEvents`；
4. `TestApiClearLogs`：校验清空日志接口同步清空时序事件数组；
5. `TestFullFlow\_Integration`集成测试：跑完完整业务链路，校验`events`数组不为空，事件正常生成。

### 关键测试说明
1. `TestMarketingCabinFallbackOpen`：单元模拟熔断器Open，验证降级兜底逻辑正常执行，不执行业务函数，降级计数+1。✅该用例PASS代表底层降级代码逻辑没问题。
2. `TestCabinReCreateLoseFallback`：复现网页重建舱丢失fallback回调的bug；PASS代表可以稳定复现该问题。
> 现象：单元测试全部PASS，网页演示降级失效，几乎都是**重建BusinessCabin忘记复制fallback回调**导致。

### 单元测试、接口测试、集成测试三者概念科普
|测试类型|测试对象|是否启动HTTP真实服务|核心目标|
|-|-|-|-|
|**单元测试**|内部零件：`BusinessCabin`、`CircuitBreaker`、`SimpleErrGroup` 结构体方法逻辑|❌不需要|验证底层零件逻辑正确性，拿到代码覆盖率；不碰http、不模拟网页操作。直接调用结构体方法，传入入参，断言返回error、状态、metrics指标。|
|**接口测试**|HTTP单个API接口|❌（使用`httptest`内存模拟http，不监听8080端口）|校验每个接口的http状态码、入参校验、返回JSON报文结构正确性；一次只测一个接口。|
|**集成测试**|完整业务链路，一串接口按顺序执行，模拟网页按钮全套操作|✅内存内完整运行整套业务逻辑|验证多个模块组合在一起整体行为；模拟用户完整操作流程，验证端到端表现。|

> 通俗大白话区分：
> 1. **单元测试**：单独测试一个个小零件，好比单独测试灯泡、开关好不好用。
> 2. **接口测试**：测试墙上插座通电是否正常，只关心单个接口本身。
> 3. **集成测试**：把整套电路全部接好，完整走一遍开灯‑关灯整套流程，看整套系统配合起来是否正常。

> 编写与调试注意事项
> 1. 每一个测试case开头，必须调用重置：清除熔断器状态、metrics、故障配置、时序事件，消除状态污染。
> 2. 单元测试不要直接调用带有随机故障模拟的业务函数`bizMarketing/bizReport`。
> 3. 集成测试内部会执行`sleep`等待熔断冷却时间，整套测试耗时会变长。
> 4. 集成测试因为业务内部随机报错，不要硬编码写死熔断器状态断言；只校验字段存在、指标范围。

## 13‑迭代记录 & 后续可迭代开发方向
### 已完成迭代
#### 迭代：手动开关 + 可配置故障比例模拟业务故障
- 营销舱、报表舱增加故障模拟配置；下单舱无故障模拟。后端新增接口`/api/setFault`；前端UI回显故障配置。

#### 迭代：日志面板‑清空日志 + 日志过滤
- 新增`/api/clearLogs`接口；仅清空日志与时序事件，不改动舱状态。
- 前端日志下拉过滤；**已完成过滤联动Canvas时序泳道图**，文本日志与画布同步过滤。

#### 迭代：Canvas时序泳道可视化
- 后端新增`TimelineEvent`结构体，`logTimelineEvent`写入结构化事件；限制最大200条。
- `/api/status`输出events数组。
- 前端Canvas基础泳道渲染，多颜色图例，鼠标悬浮tooltip。

#### 迭代：Canvas时序泳道图渲染优化（智能堆叠 + 溢出聚合）
- 同一时刻≤6事件：2行矩阵智能堆叠；球体白色描边。
- 同一时刻＞6事件：溢出聚合`+N`气泡，悬浮展示分类统计。

#### 迭代：半开试探配额控制
- 熔断器增加`halfOpenMaxProbe`、`halfOpenProbeUsed`；半开状态只放行配额内请求，配额耗尽直接拒绝，贴近工业熔断器行为。

#### 迭代：业务模拟延时配置
- 每个舱增加`NormalDelayMs`、`FaultDelayMs`；业务成功/失败分别模拟RTT，可模拟慢SQL、慢RPC。

#### 迭代：一键预设场景按钮A/B/C
- 页面增加三个场景按钮，一键重置、配置参数、设置故障，课堂演示减少手动填写。

#### 迭代：运行状态按钮置灰保护
- 批量任务执行时禁用操作按钮，防止重复点击造成指标错乱。

#### 迭代：两套重置逻辑
1. `/api/reset`完整重置全部舱、指标、熔断器、日志、时序事件。
2. `/api/resetMetricsOnly`仅重置业务指标，保留熔断器状态、窗口统计、时序事件历史。

#### 迭代：熔断器窗口统计可视化
- UI展示窗口成功、失败计数、实时失败占比，直观解释熔断双条件。

#### 迭代：舱卡片背景色跟随熔断器状态变色
- Closed浅绿 / HalfOpen浅黄 / Open浅红，投屏远距离识别故障舱。

### 后续可迭代开发方向
按优先级划分：P0优先实现，改动小，演示体验提升；P1次要；P2扩展；P3探索性功能

#### P0 优先实现｜改动小，课堂演示收益最大
* 🔲前端参数输入合法性校验：并发≥1，百分比0‑100，冷却时间≥1，半开试探数≥1；前端拦截非法输入，减少后端报错。⭐⭐⭐⭐
* 🔲熔断器Open状态网页展示冷却倒计时（距离HalfOpen剩余秒数）；解决UI快照和后端真实状态不一致演示坑。⭐⭐⭐⭐

#### P1 指标可视化｜投屏演示友好，直观展示原理
* 🔲增加时序画布缩放平移，事件过多方便浏览。⭐⭐⭐
* 🔲点击时序圆点弹窗展示完整事件详情。⭐⭐⭐
* 🔲导出实验时序事件JSON文件，课后作业保存实验记录。⭐⭐⭐

#### P2 增强功能｜贴近工业生产熔断器行为，进阶教学
* 🔲网页手动强制切换熔断器状态 Closed / Open / HalfOpen（仅限教学演示）。⭐⭐
* 🔲失败类型明细演示：区分业务参数错误和下游故障，演示哪些错误不计入熔断统计。⭐⭐☆

#### P3 深度工程拓展｜课后选学练习，课堂演示收益一般
* 🔲计数窗口切换为滑动统计窗口，对比两种窗口差异。⭐⭐☆
* 🔲失败率迷你折线图展示失败占比变化。⭐⭐
* 🔲Websocket主动推送，替换800ms轮询消除延迟。⭐
* 🔲动态新增业务隔离舱，演示舱粒度过细资源碎片化。⭐⭐

### ❌不建议投入开发方向（收益极低，不做）
1. UI主题美化、各类花哨交互动画；
2. 完整Prometheus指标上报对接，Demo项目会变得过重；
3. 登录鉴权、权限体系、复杂业务流程等无关功能。

### 优先TOP5待完成项（综合星级排序，如果只做5个）
1. 🔲前端参数输入合法性校验 ⭐⭐⭐⭐
2. 🔲熔断器Open冷却倒计时展示 ⭐⭐⭐⭐
3. 🔲画布缩放平移功能 ⭐⭐⭐
4. 🔲事件点击弹窗详情 + JSON导出 ⭐⭐⭐
5. 🔲失败类型明细与熔断统计过滤演示 ⭐⭐⭐☆

## 17‑关键代码说明
> 💡建议：**先跑通上面实验，看懂网页现象之后，再来阅读本章节源码分析。**

### 后端关键代码片段摘要
#### 17.1 熔断器状态枚举
```go
type State int32
const (
    StateClosed   State = 0 // 正常放行
    StateOpen     State = 1 // 熔断打开
    StateHalfOpen State = 2 // 半开试探
)
func (s State) String() string {
    switch s {
    case StateClosed:
        return "Closed"
    case StateOpen:
        return "Open"
    case StateHalfOpen:
        return "HalfOpen"
    default:
        return "Unknown"
    }
}
```

#### 17.2 TimelineEvent 时序事件结构体
```go
type TimelineEvent struct {
	Ts      int64  `json:"ts"`      // 毫秒时间戳
	Cabin   string `json:"cabin"`   // 舱名称
	EvType  string `json:"ev_type"` // 事件类型
	Message string `json:"msg"`     // 显示文本
}
```

#### 17.3 logTimelineEvent 写入结构化事件函数
```go
func logTimelineEvent(cabinName string, evType string, msg string) {
	onEventLog(msg) // 保留原有文本日志输出
	timelineMu.Lock()
	defer timelineMu.Unlock()
	evt := TimelineEvent{
		Ts:      time.Now().UnixMilli(),
		Cabin:   cabinName,
		EvType:  evType,
		Message: msg,
	}
	globalTimelineEvents = append(globalTimelineEvents, evt)
	// 限制最大条数，防止内存暴涨
	if len(globalTimelineEvents) > 200 {
		globalTimelineEvents = globalTimelineEvents[len(globalTimelineEvents)-200:]
	}
}
```

#### 17.4 BusinessCabin 水密舱结构体
```go
type BusinessCabin struct {
    name           string
    maxConcurrency int64
    sem            *semaphore.Weighted
    cb             *CircuitBreaker
    fallback       func(ctx context.Context) error // 降级回调，nil代表不启用降级
    metrics        CabinMetrics
    mu             sync.Mutex
	normalDelayMs int
	faultDelayMs  int
}
```

#### 17.5 业务执行入口 Run（熔断+降级+限流完整链路）
执行顺序：**先判断熔断器 → 如果 Open 并且存在 fallback，执行降级兜底 → 再做信号量并发限流 → 执行业务 → 将成功失败结果反馈给熔断器，同时记录时序事件**

> ⚠ **重要坑：c.cb.Allow() 不是只读查询函数，具备状态变更副作用！**
> Open状态时间到期，Allow()内部自动将状态切换为HalfOpen，返回 true 放行请求。网页 UI 只是状态快照，不等于真实运行时状态。

```go
func (c *BusinessCabin) Run(ctx context.Context, fn func(ctx context.Context) error) error {
	c.mu.Lock()
	c.metrics.TotalTask += 1
	c.mu.Unlock()
	if !c.cb.Allow() {
		if c.fallback != nil {
			c.mu.Lock()
			c.metrics.DegradeCount += 1
			c.mu.Unlock()
			logTimelineEvent(c.name, "fallback", fmt.Sprintf("[%s] 触发降级兜底，返回缓存活动数据", c.name))
			return c.fallback(ctx)
		}
		c.mu.Lock()
		c.metrics.CbReject += 1
		c.mu.Unlock()
		return errors.New("cabin circuit breaker open: service reject")
	}
	// 信号量满，限流拒绝
	if !c.sem.TryAcquire(1) {
		c.mu.Lock()
		c.metrics.LimitReject += 1
		c.mu.Unlock()
		logTimelineEvent(c.name, "reject", fmt.Sprintf("[%s]信号量满，并发限流拒绝", c.name))
		return errors.New("cabin semaphore full: concurrency limit reject")
	}
	defer c.sem.Release(1)
	var execErr error
	func() {
		ctxT, cancel := context.WithTimeout(ctx, 3*time.Second)
		defer cancel()
		execErr = fn(ctxT)
	}()
	// 舱配置驱动延时
	var delay time.Duration
	if execErr != nil {
		delay = time.Duration(c.faultDelayMs) * time.Millisecond
	} else {
		delay = time.Duration(c.normalDelayMs) * time.Millisecond
	}
	if delay > 0 {
		select {
		case <-time.After(delay):
		case <-ctx.Done():
		}
	}
	if execErr != nil {
		c.mu.Lock()
		c.metrics.BusinessError += 1
		c.mu.Unlock()
		logTimelineEvent(c.name, "error", fmt.Sprintf("[%s]业务执行报错", c.name))
	} else {
		c.mu.Lock()
		c.metrics.Success += 1
		c.mu.Unlock()
		logTimelineEvent(c.name, "normal", fmt.Sprintf("[%s]业务执行成功", c.name))
	}
	c.cb.OnResult(execErr != nil)
	return execErr
}
```

> **关键点：重建 BusinessCabin 结构体的时候，NewCabin会把fallback置nil，必须手动赋值复制旧对象的fallback回调函数。**

#### 17.6 熔断器 OnResult 状态流转回调
业务执行完成后回调，根据成功失败切换熔断器状态；状态变更处调用`logTimelineEvent()`记录熔断事件。
```go
func (cb *CircuitBreaker) OnResult(isFail bool) {
	succ := cb.successCount.Load()
	fail := cb.failureCount.Load()
	total := succ + fail
	if total >= cb.windowSize {
		cb.successCount.Store(0)
		cb.failureCount.Store(0)
		succ, fail, total = 0, 0, 0
	}
	if isFail {
		cb.failureCount.Add(1)
		fail++
	} else {
		cb.successCount.Add(1)
		succ++
	}
	total = succ + fail
	st := State(cb.state.Load())
	switch st {
	case StateClosed:
		if total > 0 {
			failRate := fail * 100 / total
			if failRate >= cb.thresholdFailurePct {
				cb.state.Store(int32(StateOpen))
				cb.lastOpenTime.Store(time.Now().UnixMilli())
				logTimelineEvent("熔断器全局", "open", "熔断器切换 Open（熔断打开）")
			}
		}
	case StateHalfOpen:
		if isFail {
			cb.state.Store(int32(StateOpen))
			cb.lastOpenTime.Store(time.Now().UnixMilli())
			logTimelineEvent("熔断器全局", "open", "半开试探失败，重新切回 Open 熔断")
		} else {
			cb.state.Store(int32(StateClosed))
			logTimelineEvent("熔断器全局", "halfOpen", "半开试探成功，恢复 Closed 正常状态")
		}
	case StateOpen:
	}
}
```

#### 17.7 故障注入判断逻辑
```go
func shouldProduceFault(cabinName string) bool {
    faultMu.Lock()
    defer faultMu.Unlock()
    cfg, ok := faultConfig[cabinName]
    if !ok || !cfg.Enable {
        return false
    }
    p := cfg.FaultPercent
    if p <= 0 {
        return false
    }
    if p >= 100 {
        return true
    }
    return rand.Intn(100) < p
}
```

### 前端关键代码摘要
> 💡前端不实现限流熔断业务逻辑，全部数据来自后端`/api/status`接口。

#### 全局事件与颜色映射
```js
let eventList = [];
const colorMap = {
    normal:"#67c23a",
    reject:"#e6a23c",
    open:"#f56c6c",
    halfOpen:"#409eff",
    fallback:"#909399",
    error:"#ff4d4f",
    config:"#9b59b6"
};
```

#### fetchStatus 轮询主入口
```js
async function fetchStatus(){
    // 请求 /api/status 获取后端全部状态
    // 1.拿到cabins舱数据、events时序事件数组
    // 2.舱DOM数量变化则完整rebuildCabinsDOM()，否则做增量局部更新指标、状态、卡片底色
    // 3.document.activeElement判断输入框焦点，编辑状态不覆盖输入框value
    // 4.renderAllLogs()渲染过滤后的文本日志
    // 5.renderTimelineSvg()绘制Canvas泳道时序图
}
```

#### renderTimelineSvg Canvas时序泳道图核心流程
1. 判空；根据`logFilterMode`过滤事件。
2. 提取舱名称生成Y轴泳道；计算事件时间范围，自适应X轴时间刻度。
3. 绘制泳道背景、舱名称、垂直网格时间线。
4. 事件分组：50ms时间窗口+舱名作为key，把相近时间事件归为一组。
5. 分组分支：
    - 事件数量＞6：溢出聚合模式，绘制`+N`气泡，统计各事件类型，保存tooltip。
    - 事件数量≤6：智能2行矩阵堆叠绘制圆点，每个圆点带白色描边；记录鼠标检测区域hitAreas。
6. 绑定canvas鼠标移动、离开事件；hover命中区域展示tooltip提示。

#### 按钮运行锁，防止重复点击
```js
let isRunning = false;
function setButtonsDisabled(disabled) {
    // 将场景按钮、流量操作按钮置灰/启用
}
async function runFirst(){
    if(isRunning) return;
    isRunning = true; setButtonsDisabled(true);
    try{
        await fetch("/api/runFirst");
        await fetchStatus();
    }finally{
        isRunning = false; setButtonsDisabled(false);
    }
}
```

#### 页面初始化
```js
window.onload = async function(){
    await fetchStatus();
    setInterval(fetchStatus, 800); //800ms轮询后端状态
    document.getElementById("logFilterSel").onchange = function(){
        logFilterMode = this.value;
        fetchStatus();
    }
};
```

