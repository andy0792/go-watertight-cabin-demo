# 软件水密隔舱（限流+熔断+降级+故障注入）学习文档.md
> 项目名称：go‑watertight‑cabin‑demo
>
> 这是一个Go实现的网页可视化教学Demo，演示**水密隔舱模式（Bulkhead水密隔舱）**，结合**信号量并发限流 + 熔断器CircuitBreaker + 业务降级Fallback + 网页故障注入**。非开发人员仅靠点击按钮、修改网页数字就可以理解分布式故障隔离原理。
>
> 类比轮船水密隔舱：单个舱故障进水，不会造成整艘船沉没；业务模块故障，不会把整个应用拖垮，实现故障隔离。
## 💡分布式保护三板斧：限流、熔断、降级
这三个是微服务高可用最经典的 **“三板斧”**，用来防雪崩，避免一个服务挂掉拖垮整个链路。
> 💡分布式高可用保护三板斧：**限流、熔断、降级**
>
> - **限流**：抵御突发大流量，控制并发/请求量，超出阈值直接拒绝请求，保护自身服务不被流量打垮；属于“防外部流量涌入”。
> - **熔断**：感知下游依赖大量报错、超时，切断对故障下游的调用，避免无效调用堆积资源，防止故障链式扩散；属于“防下游故障传染”。熔断器具备 Closed(正常)‑Open(熔断)‑HalfOpen(半开试探) 状态流转。
> - **降级**：当系统压力大或依赖故障时，主动舍弃非核心业务逻辑，返回兜底数据/简化逻辑，保障核心业务可用；属于“自身主动取舍保核心”。
>
> 三者配合分工：**限流挡流量，熔断断坏依赖，降级保核心业务**。本Demo完整实现限流、熔断、**熔断触发后的fallback业务降级兜底**，同时支持网页端可视化故障注入，不用改代码即可模拟下游各种故障工况。
>
> 💡容易混淆点：熔断是手段，降级往往是熔断触发之后执行的兜底行为。熔断器打开之后，一般就执行降级逻辑返回兜底结果。
#### 💡一句话区分
- **限流**：防流量太大，管入口，挡住突发流量，保护自己不被打垮。
- **熔断**：防雪崩，管下游，下游大量报错就切断调用，不再访问故障依赖。
- **降级**：防自己扛不住，管自身，舍弃非核心逻辑，返回兜底结果保核心可用。
> 💡记忆口诀：**限流挡流量，熔断断下游，降级保自己**。
### 熔断器状态流转图（文字版）
```
Closed正常（允许访问）
        ↓（错误占比超过阈值）
Open熔断阻断（拒绝访问）
        ↓（等待熔断冷却时间到期）
HalfOpen试探恢复（小流量测试）
    ├─→ 业务调用成功 → 回到 Closed正常（允许访问）
    └─→ 业务调用失败 → 回到 Open熔断阻断（拒绝访问）
```
> 文字描述：
>
> 1. **Closed（正常）**：正常接收业务请求，统计窗口内持续统计成功/失败；当失败占比达到设定阈值，切换到 Open。
> 2. **Open（熔断阻断）**：直接拒绝真实下游调用；**营销舱命中降级兜底逻辑**，返回缓存数据；其它舱直接返回熔断拒绝错误。
> 3. **等待冷却窗口期**：Open状态持续等待配置的冷却时间，到期自动进入 HalfOpen。
> 4. **HalfOpen（试探恢复）**：放行少量试探流量去访问真实下游服务。
>    - 试探请求成功：熔断器判定下游已恢复，切回 Closed，恢复全部流量；
>    - 试探请求失败：判定下游依旧故障，立刻切回 Open，继续阻断请求。
---
## 目录
1. [项目功能简介](#1‑项目功能简介)
2. [核心概念通俗讲解](#2‑核心概念通俗讲解)
3. [系统整体架构](#3‑系统整体架构)
4. [编译&运行命令说明](#4‑编译运行命令说明)
5. [网页界面说明](#5‑网页界面说明)
6. [完整演示实验操作指南](#6‑完整演示实验操作指南)
7. [调参实验参考](#7‑调参实验参考)
8. [优缺点与适用场景](#8‑优缺点与适用不适用场景)
9. [课堂演示话术（可直接复制讲解）](#9‑课堂演示话术可直接复制讲解)
10. [生产环境注意事项](#10‑生产环境注意事项与坑点)
11. [常见现象答疑](#11‑常见现象答疑)
12. [单元测试、接口测试、集成测试执行与命令说明](#12‑单元测试接口测试集成测试执行与命令说明)
13. [迭代：手动开关 + 可配置故障比例模拟业务故障（已完成）](#13‑迭代手动开关--可配置故障比例模拟业务故障已完成)
14. [迭代：日志面板‑清空日志 + 日志过滤（已完成）](#14‑迭代日志面板‑清空日志--日志过滤已完成)
15. [后续可迭代开发方向](#15‑后续可迭代开发方向)
16. [关键代码说明](#16‑关键代码说明)
> 说明：文档面向非开发人员，阅读顺序：先看懂项目介绍 → 部署运行 → 网页操作 → 做实验体验现象，**最后再阅读关键代码剖析实现原理**。
## 1 项目功能简介
每个业务拥有独立隔离舱，舱与舱之间完全隔离互不影响，提供保护机制：
1. **信号量并发限流**：控制单个业务最大并发请求，超过上限直接快速拒绝，不堆积协程。
2. **熔断器故障熔断**：统计业务请求的成功、失败，失败占比达到阈值触发熔断，拒绝新请求，防止故障雪崩。
3. **熔断冷却+半开试探恢复**：熔断经过冷却时间，进入半开状态，放行请求探测业务是否已经恢复；试探成功恢复正常；试探失败立刻再次熔断。
4. **业务降级Fallback**：营销舱支持降级，熔断器Open时执行兜底函数返回缓存数据，不再调用真实业务；下单、报表舱无降级。
5. **网页故障注入**：营销舱、辅助舱‑报表支持网页端配置故障开关、故障百分比；无需修改代码、无需重新编译，模拟下游正常、不稳定、完全故障。核心舱‑下单不支持故障注入，始终业务正常。
6. Web可视化界面：实时查看舱状态、各项统计指标；网页直接修改参数；事件日志实时输出。
> 内置3个业务舱，分工设定
- **核心舱‑下单**：演示**限流功能**。模拟核心业务，核心交易，无降级；失败就返回错误，保证数据真实；**无故障注入开关，下游永远正常**。
- **营销舱**：演示**降级Fallback**功能，模拟普通业务，当熔断器判定下游异常时，执行降级逻辑，返回兜底数据，不向上层抛 error；**支持故障注入开关、故障百分比配置**。
- **辅助舱‑报表**：演示**熔断功能**，模拟容易故障的下游服务（慢 SQL，大量报错），用来演示熔断、故障隔离；**支持故障注入开关、故障百分比配置，无降级兜底**。
## 2 核心概念通俗讲解
| 概念            | 通俗比喻                         | 网页UI标识                   |
| ------------- | ---------------------------- | ------------------------ |
| 水密隔舱 Bulkhead | 轮船独立隔间，一个隔间故障，其他隔间不受影响       | 3张独立业务卡片面板               |
| 并发限流（信号量）     | 窗口最多同时接待N个人，人多直接告知繁忙，不全部堵在门口 | 参数：最大并发                  |
| 熔断器 Closed    | 业务健康，全部请求放行                  | 🟢 Closed正常（允许访问）        |
| 熔断器 Open      | 业务故障，拒绝新来请求，阻止故障扩散           | 🔴 Open熔断阻断（拒绝访问）        |
| 熔断器 HalfOpen  | 冷却结束，放少量请求试探业务是否修复           | 🟡 HalfOpen试探恢复（小流量测试）   |
| 降级Fallback    | 熔断打开，不走真实业务，返回缓存兜底数据         | 指标：降级次数                  |
| 故障注入          | 网页开关模拟下游故障概率，0‑100%可调        | 每个舱面板：模拟业务故障勾选框 + 故障%输入框 |
| 故障隔离          | 报表业务故障熔断，下单、营销业务依旧正常工作，不会被拖垮 | 三个舱状态互不干扰                |
> ⚠重要区分：
>
> **限流拒绝 ≠ 业务报错 ≠ 降级**
>
> - 限流拒绝：并发已满，请求被挡在舱入口，业务逻辑完全没有执行，不计入熔断器失败统计。
> - 业务报错：请求成功进入舱并执行业务，代码抛出错误，这个才会参与熔断器失败率计算。
> - 降级：熔断器Open，执行fallback兜底函数，**完全不执行真实业务**，返回成功兜底结果。
## 3 系统整体架构
```
浏览器网页前端
        ↓ HTTP接口
Go后端服务
├‑3个 BusinessCabin 水密业务舱（互相完全隔离）
│  ├‑核心舱‑下单（无降级、无故障注入）
│  ├‑营销舱（支持 fallback降级 + 网页故障注入）
│  └‑辅助舱‑报表（无降级 + 网页故障注入）
│
├‑CircuitBreaker熔断器：Closed / Open / HalfOpen三状态机
├‑semaphore信号量：每个舱独立控制最大并发
├‑SimpleErrGroup：协程组，批量发起模拟请求
├‑全局faultConfig故障配置：保存各舱故障开关、故障百分比（内存存储，重启丢失）
└‑Metrics指标：每个舱独立统计：总任务、成功、限流拒绝、熔断拒绝、业务报错、**降级次数DegradeCount**
```
网页前端与后端接口：
| 接口                  | 作用                                         |
| ------------------- | ------------------------------------------ |
| `/api/status`       | 获取全部舱状态、指标、事件日志、故障配置，前端800ms定时拉取刷新页面       |
| `/api/setFault`     | 设置故障注入：舱名称、故障开关enable、故障百分比faultPercent    |
| `/api/runFirst`     | 发起第一轮批量模拟请求                                |
| `/api/waitHalfOpen` | 等待熔断恢复窗口期                                  |
| `/api/runSecond`    | 发起第二轮试探请求                                  |
| `/api/reset`        | 重置全部舱，指标清零、日志清空，恢复初始状态                     |
| `/api/applyConfig`  | 接收网页参数，重建业务舱，动态更新配置（重建舱需要手动复制fallback回调函数） |
| `/api/clearLogs`    | 仅清空事件日志，**不修改舱指标、熔断器状态**                     |
> 前端渲染优化说明：
>
> 前端不再无条件执行`innerHTML`全量销毁重建DOM；
>
> - 仅舱卡片数量变化才全量重建DOM；
> - 平时只局部更新指标、熔断器状态文本；
> - 使用`document.activeElement`焦点判断，**光标在输入框/复选框编辑时，不会被轮询刷掉用户输入**。
>
> 局限：输入框失去焦点但未点击按钮提交，下一轮轮询会被后端旧配置回填。演示场景建议修改参数后立刻点击按钮提交。
## 4 编译&运行命令说明
> 🎯非开发人员优先阅读本节：拿到源码第一步，先把项目跑起来，先看效果再研究代码。
>
> 场景：把整套Go源码复制到全新文件夹（示例路径：`D:\go-cabin`），只拷贝`.go`文件会报模块缺失错误：
> `go-go.go:14:2: no required module provides package golang.org/x/sync/semaphore: go.mod file not found in current directory or any parent directory; see 'go help modules'`
Windows CMD完整操作：
```cmd
# 进入新的工程目录
cd /d D:\\go-cabin
# 1.初始化go模块，模块名可以自定义，可与github仓库名保持一致
go mod init go-cabin
# 配置国内代理，解决依赖拉取网络失败
go env -w GOPROXY=https://goproxy.cn,direct
# 2.自动扫描代码，下载补齐全部第三方依赖，生成go.mod、go.sum
go mod tidy
# 源码方式运行项目
go run go-go.go
```
浏览器访问：`http://127.0.0.1:8080`，建议无痕窗口，避免浏览器缓存干扰页面渲染。
```cmd
# 编译生成 exe 可执行程序 go‑cabin.exe
go build .
```
### 单元测试全套命令（开发调试使用）
```cmd
# 基础单元测试，-p 1串行执行，避免全局状态互相污染
go test -v -p 1
# 带超时、覆盖率执行测试
go test -v -p 1 -cover -timeout 20s
# 生成覆盖率统计文件 cover.out
go test -v -p 1 -coverprofile=cover.out
# 浏览器打开可视化覆盖率报告
go tool cover -html=cover.out
```
### 常见踩坑
❌ 只拷贝 `.go`源码，不带 mod 文件，就报 `no required module provides package`。
✅ 项目完整交付 = 全部`.go` + `go.mod` + `go.sum`，这三个必须一起迁移。
> 迁移方案二：如果旧项目已经存在`go.mod`、`go.sum`，直接把这两个文件复制到新目录，跳过`go mod init`，直接执行`go mod tidy`即可。
## 5 网页界面说明
> 页面说明文字（已优化，直接使用）
```
说明：每个业务独立隔离舱，通过信号量限制最大并发请求；请求失败占比超标自动触发熔断器。
Closed正常（允许访问）【绿色】：正常放行请求
HalfOpen试探恢复（小流量测试）【黄色】：放行少量请求探测下游是否恢复
Open熔断阻断（拒绝访问）【红色】：直接拒绝新的业务请求
📌营销舱降级演示避坑要点：
✔营销舱熔断冷却时间改成30秒
✔开启模拟业务故障，故障%=100，点设置故障
✔请求打到红色Open熔断后，**禁止点重置**，再次发请求，观察降级次数上涨。
备注：营销舱：支持故障注入 + 降级兜底；报表舱：支持故障注入，无降级；核心舱‑下单：无故障注入开关。
```
### 参数输入框（每个业务舱独立配置）
| 参数        | 说明                           |
| --------- | ---------------------------- |
| 最大并发      | 该业务舱允许同时处理的请求上限，超过直接限流拒绝     |
| 失败触发熔断%   | 统计样本内，失败请求占比达到该百分比就触发熔断      |
| 统计样本请求数   | 需要收集多少条请求作为判断样本；样本数量不足不会触发熔断 |
| 熔断冷却时间(s) | 熔断打开之后，等待多少秒，才进入半开试探阶段       |
> 🎯**降级演示小技巧**：做PPT演示营销舱降级，把营销舱熔断冷却时间设置为 **30s**；打到Open红色之后，**30秒之内立刻发起新一轮请求**，才能命中降级。如果等待超过冷却时间，Allow自动切HalfOpen放行，降级不会触发。
### 故障注入面板（营销舱、辅助舱‑报表可见）
- `模拟业务故障`勾选框：故障总开关，勾选才会启用故障百分比；
- `故障%`：0‑100，100代表每次请求必模拟业务报错；
- 【设置故障】按钮：修改完成必须点击按钮，配置才提交后端生效。
### 舱状态展示
- 🟢 `Closed正常（允许访问）`：正常状态，全部请求放行
- 🟡 `HalfOpen试探恢复（小流量测试）`：半开，放行请求探测业务是否恢复
- 🔴 `Open熔断阻断（拒绝访问）`：熔断生效，直接拒绝新请求
### 统计指标
- 总任务：进入本舱的全部请求数量
- 成功：业务逻辑正常返回成功
- 限流拒绝：并发已满，被信号量拦截（业务未执行）
- 熔断拒绝：熔断器打开，无降级时拒绝请求
- 业务报错：请求已经进入舱执行业务，业务抛出异常
- **降级次数：熔断器Open，执行fallback兜底的请求计数**
#### 熔断器窗口统计展示
每个业务舱卡片指标区域额外展示熔断器窗口内部实时统计：
> `窗口统计：成功:N 失败:N 当前失败占比:XX%`
> 熔断触发必须**两个条件同时满足**：
> 1. 窗口总请求数（成功+失败）≥【统计样本请求数】
> 2. 当前失败占比 ≥【失败触发熔断%】
>
> 💡现象说明：当请求填满【统计样本请求数】配置值，窗口计数器会自动清零，属于熔断器窗口重置的正常逻辑。
#### 舱卡片状态背景色
舱卡片整体底色跟随熔断器状态同步变化（800ms轮询自动更新）：
- 🟢 Closed正常：卡片**浅绿色底色**
- 🟡 HalfOpen试探恢复：卡片**浅黄色底色**
- 🔴 Open熔断阻断：卡片**浅红色底色**
> 投屏演示时，无需细读文字，通过卡片底色即可一眼识别哪个舱发生熔断，直观体现水密隔舱故障隔离效果。
#### 按钮运行置灰保护
执行批量请求、一键场景配置过程中，流量操作按钮与场景按钮会自动置灰不可点击，**防止重复触发任务造成metrics指标错乱**；【重置所有舱状态】按钮始终可用，可以随时打断正在执行的任务。
### 日志面板控件
日志面板上方提供两个控件：
1. **【清空日志】红色按钮**：仅清空网页日志面板内容；**不会重置舱指标、不会修改熔断器状态**。适合一轮实验结束，清理历史日志干扰，从头观察新一轮请求输出。
> 区分：【④ 重置所有舱状态】会清零指标、重置熔断器、同时清空日志；而清空日志按钮只清理日志，保留全部运行状态。
2. **日志过滤下拉选择器**
    - `全部日志`：展示完整日志，包含业务请求、限流、熔断器、降级全部事件；
    - `仅熔断器事件`：只展示熔断器状态切换、半开试探相关日志，屏蔽大量普通业务刷屏，专注观察状态流转；
    - `仅降级事件`：只输出降级兜底事件日志，演示营销舱降级场景时使用。
> 实现说明：过滤为前端文本关键词过滤，不修改后端日志存储；切换过滤条件即时生效，800ms轮询刷新会保持当前过滤选项。
### 页面按钮
| 按钮 | 功能 |
|------|------|
| ① 发起第一轮批量请求 | 发送批量模拟请求，用于制造故障场景 |
| ② 等待熔断恢复窗口期 | sleep等待，观察熔断器是否切换半开状态 |
| ③ 发起第二轮试探请求 | 半开状态下发送请求，探测业务是否恢复 |
| ④ 重置所有舱状态 | 指标清零、日志清空，全部舱恢复绿色初始状态 |
| 【清空日志】 | 只清空日志面板，保留舱指标、熔断器状态不变，用于多轮实验清理日志 |
> 修改舱参数之后，点击蓝色【应用配置】新参数才会生效；注意重建营销舱时代码要复制 fallback 降级回调。
> 修改故障注入参数，点击红色【设置故障】提交后端生效。
> 【④重置所有舱状态】会指标清零、日志清空，全部舱恢复绿色初始状态；区别于「清空日志」按钮，重置会修改熔断器与指标。
## 6 完整演示实验操作指南
> 🎯非开发人员优先做本节，跑3个实验，直观看懂限流、熔断、降级现象。
> 💡课堂演示优先使用紫色一键场景按钮，省去手动填写参数，节省演示时间；需要讲解参数含义时，使用手动调参流程。
> 💡观察提示：做熔断相关实验，除了看状态文字，同时留意**舱卡片底色**与**窗口统计行数值**。
> 熔断需要同时满足「样本数量足够」+「失败占比达标」两个条件，不要只看业务报错数量。
> 💡投屏演示小技巧：
> 演示熔断器状态流转，可以将日志过滤切换为【仅熔断器事件】；
> 演示降级现象切换【仅降级事件】；实验开始前点【清空日志】，排除上一轮残留日志干扰。

### 🧪实验一：演示【并发限流】
> 目标：观察并发超限出现限流拒绝，**熔断器保持绿色，不会触发熔断**。
✅ **方式 1：一键场景快速操作（推荐课堂演示）**
1. 点击紫色按钮【场景 A‑限流压力测试】，程序自动完成重置，设置核心舱‑下单最大并发 = 2，关闭全部故障注入。
2. 点击 ① 发起第一轮批量请求。
**观察现象：**
- 状态依旧 🟢 绿色正常
- 限流拒绝计数上涨，成功等于设置的最大并发
- **原理：** 并发已满直接拒绝，业务没有执行，不会产生业务报错，熔断器不会触发熔断。
✅ **方式 2：手动调参操作**
1. 点击【④重置所有舱状态】清零；
2. 修改【核心舱‑下单】参数：
   - 最大并发：5；失败触发熔断%：50；统计样本请求数：40；熔断冷却时间(s)：1；点击【应用配置】
3. 点击`① 发起第一轮批量请求`，会一次性发起10个下单请求。
4. 观察现象：
   - 状态依旧🟢绿色正常；
   - `限流拒绝`计数上涨，成功等于最大并发5；
> 原理：并发已满直接拒绝，业务没有执行，不会产生业务报错，熔断器不会触发熔断。
### 🧪实验二：完整演示熔断器生命周期（重点）
> 使用【辅助舱‑报表】，开启故障注入，故障%=100，模拟下游完全故障。
✅ **方式 1：一键场景快速操作（推荐课堂演示）**
1. 点击紫色按钮【场景 C‑报表舱故障隔离】，自动重置，报表舱配置完成，报表故障 100% 开启，营销舱故障关闭。
2. 点击 ① 发起第一轮批量请求。
**现象：** 业务报错持续上涨；样本收集完成，失败占比超过阈值，状态切换 🔴 红色 Open 熔断阻断。
3. 点击 ② 等待熔断恢复窗口期，冷却时间结束，状态变为 🟡 黄色 HalfOpen 半开试探。
4. 点击 ③ 发起第二轮试探请求：
   - **分支 A（故障保持开启）：** 试探请求出现报错 → 立刻切回 🔴 红色熔断
   - **分支 B（关闭故障）：** 在界面报表舱取消勾选模拟业务故障，点击设置故障；再执行③，试探全部成功 → 切回 🟢 绿色正常，熔断解除
> ✨ **核心演示现象：** 报表舱故障熔断，下单舱、营销舱完全不受影响，实现故障隔离（水密隔舱核心思想）。
5. 实验结束，点击【④ 重置所有舱状态】。
✅ **方式 2：手动调参操作**
1. 点击【④重置所有舱状态】回到干净初始状态；
2. 报表舱：勾选`模拟业务故障`，故障%=`100`，点击【设置故障】；
3. 参数参考：最大并发8，失败触发熔断%：40，统计样本请求数：30，熔断冷却时间(s)：1，【应用配置】；
4. 点击`① 发起第一轮批量请求`；
5. 现象：业务报错持续上涨；样本收集完成，失败占比超过阈值，状态切换🔴红色 `Open熔断阻断`；
6. 点击`② 等待熔断恢复窗口期`，冷却时间结束，状态变为🟡黄色`HalfOpen半开试探`；
7. 点击`③ 发起第二轮试探请求`：
   - 分支A（故障保持开启）：试探请求出现报错 → 立刻切回🔴红色熔断；
   - 分支B（关闭故障：取消勾选模拟业务故障）：试探全部成功 → 切回🟢绿色正常，熔断解除。
> ✨核心演示现象：报表舱故障熔断，**下单舱、营销舱完全不受影响，实现故障隔离（水密隔舱核心思想）**。
8. 实验结束，点击【④重置所有舱状态】。
### 🧪实验三：演示【营销舱熔断+降级Fallback】
> 前提：营销舱熔断冷却时间设置为 **30秒**（PPT演示拉长窗口期，避免Allow自动切HalfOpen）。
✅ **方式 1：一键场景快速操作（推荐课堂演示）**
1. 点击紫色按钮【场景 B‑营销舱熔断 + 降级演示】；自动重置，营销舱冷却 30s，故障 100% 开启，报表舱故障关闭。
2. 反复点击 ① 发起第一轮批量请求，直到营销舱状态变为 🔴 红色 Open 熔断阻断。
3. ⚠️ **必须在 30 秒冷却窗口期以内，立刻再次点击【① 发起第一轮批量请求】**
**预期现象：**
- 控制台输出：`[营销舱‑降级兜底] 返回缓存活动数据`
- 网页营销舱面板：**降级次数数字上涨，熔断拒绝(CbReject)保持0**；
- 不会执行真实`bizMarketing`业务函数，不会打印业务SUCCESS/DB超时报错。
> ⚠坑：如果等待超过30秒再点击，`Allow()`内部自动切换HalfOpen放行，降级不会触发。
✅ **方式 2：手动调参操作**
1. 点击【④重置所有舱状态】清零；
2. 修改营销舱参数：熔断冷却时间(s)=30，点击【应用配置】；
3. 营销舱：勾选`模拟业务故障`，故障%=`100`，点击【设置故障】；
4. 反复点击`① 发起第一轮批量请求`，直到营销舱状态变为🔴红色`Open熔断阻断`；
5. ⚠️**必须在30秒冷却窗口期以内，立刻再次点击【①发起第一轮批量请求】**；
6. 预期现象：
   - 控制台输出：`[营销舱‑降级兜底] 返回缓存活动数据`
   - 网页营销舱面板：**降级次数数字上涨，熔断拒绝(CbReject)保持0**；
   - 不会执行真实`bizMarketing`业务函数，不会打印业务SUCCESS/DB超时报错。
> ⚠坑：如果等待超过30秒再点击，`Allow()`内部自动切换HalfOpen放行，降级不会触发。
### 页面按钮说明（实验配套）
| 按钮 | 功能 |
|------|------|
| ① 发起第一轮批量请求 | 发送批量模拟请求，用于制造故障场景 |
| ② 等待熔断恢复窗口期 | sleep 等待，观察熔断器是否切换半开状态 |
| ③ 发起第二轮试探请求 | 半开状态下发送请求，探测业务是否恢复 |
| ④ 重置所有舱状态 | 指标清零、日志清空，全部舱恢复绿色初始状态 |
| 【清空日志】 | 只清空日志面板，保留舱指标、熔断器状态不变，用于多轮实验清理日志 |
> 修改舱参数之后，点击蓝色【应用配置】新参数才会生效；
> 修改故障注入参数，点击红色【设置故障】提交后端生效。
## 7 调参实验参考
修改参数，点击应用配置，再跑完整流程，观察现象。
1. **调高失败触发熔断%=80**：需要80%请求失败才熔断；同样大量报错，失败率只有66%，不会触发红色熔断。
2. **调高统计样本请求数=100**：需要攒够100条请求样本；只发送25条请求，样本不足，无论报错多少，不会熔断。
3. **调高熔断冷却时间(s)=30**：熔断变红后，需要等待30秒才进入半开状态；**降级演示必须调大这个参数**。
4. **调大最大并发=20**：限流拒绝变少，更多请求可以执行业务，更容易积累报错触发熔断。
5. **故障注入调参**
   - enable=true，faultPercent=100：每次必报错，快速打熔断；
   - enable=true，faultPercent=50：一半成功一半失败，可以观察熔断器震荡；
   - enable=true，faultPercent=0：开关打开但是业务全部成功，模拟下游故障恢复。
> 关键理解：熔断触发需要**两个条件同时满足**：①收集足够统计样本；②失败占比达到阈值。
## 8 优缺点与适用/不适用场景
### ✅优点
1. **故障隔离，阻止雪崩（核心价值）**：单个业务故障，只会消耗本舱的并发配额，不会抢占其他业务资源；局部故障不会演变为整个服务雪崩。
2. **熔断配合降级，保留部分业务可用**：非核心业务故障熔断，执行fallback返回兜底缓存数据，保障接口不直接报错。
3. **快速失败，避免请求堆积**：信号量满直接拒绝；熔断打开优先执行降级/快速返回错误，减少无效等待、goroutine堆积。
4. **每个舱独立配置策略**：核心业务、非核心业务可以设置不同的并发、熔断阈值、降级回调。
5. **可观测**：每个舱独立统计指标（含降级次数），便于定位故障点。
6. **可视化故障注入**：网页界面即可模拟下游多种故障工况，不需要修改源码、重新编译，教学演示非常方便。
### ⚠缺点与坑
1. **资源碎片化**：舱划分越多，资源越碎片化；空闲舱的并发配额，不能借给流量突增的其他舱使用，资源利用率下降。
2. **参数调优难度高**：参数设置错误会出现误限流、频繁熔断器震荡（Open ↔ HalfOpen来回跳动），需要压测结合业务P99延迟调参。
3. **仅进程内防护**：只能防护下游慢调用耗尽本进程goroutine、连接；**无法防护CPU打满、OOM、机器宕机**，不能替代K8s容器资源限制。
4. **需要区分错误类型**：Demo把所有error都统计为失败；生产中用户参数错误、鉴权失败属于业务逻辑错误，**不应该计入熔断统计**，否则会误触发熔断。
5. **Demo半开逻辑简化**：生产熔断器半开只放行少量试探请求；本Demo半开放行全部请求，如果下游刚恢复，大量流量会直接再次打垮下游。
6. **回调函数容易丢失**：重建`BusinessCabin`结构体，NewCabin会把fallback置nil，必须手动复制fallback回调；单元测试可以复现，网页演示很容易踩这个坑。
7. **增加代码复杂度**，小简单系统会造成过度设计。
8. **故障注入配置内存存储**：进程重启全部丢失，仅教学演示，不可上生产。
### 🎯适合场景
1. 微服务，调用多个外部下游RPC、DB、第三方HTTP接口，下游存在慢、超时风险；
2. 系统区分核心业务、非核心业务，希望非核心故障不影响核心链路，熔断后执行本地降级兜底；
3. 存在报表、导出等重风险慢业务；
4. 需要防止慢调用耗尽goroutine、数据库连接池，避免服务雪崩。
### ❌不建议使用场景
1. 简单单体小项目，外部依赖很少，属于过度设计；
2. 故障根源是CPU/OOM进程整体资源耗尽，本模式无法解决；
3. 以消息队列异步消费为主的系统，隔离逻辑更多放在消费者侧。
## 9 课堂演示话术（面向非开发人员，直接照着讲）
> **开场介绍**
> “今天演示分布式系统两个重要保护机制：并发限流和熔断器，这个模型叫做水密隔舱，来自轮船设计。轮船划分多个隔间，一个隔间漏水，不会整艘船沉没。对应软件系统，某个业务模块故障，不能把整个系统拖垮。
>
> 我们这里有三个舱：核心舱‑下单、营销舱、辅助舱‑报表。报表、营销舱可以在网页打开模拟业务故障，模拟下游服务不稳定或者完全挂掉。营销舱额外实现了熔断后的降级兜底。
>
> 界面状态：🟢绿色正常放行，🟡黄色小流量试探，🔴红色熔断拒绝。”
>
> **演示一：并发限流**
> “现在我们把下单舱最大并发设置为5，同一时间最多处理5个请求，一次性发起10个请求。
>
> 大家可以看到限流拒绝计数上涨，但是状态依旧是绿色。
>
> 👉重点：限流不等于业务报错。请求太多，直接挡在门外，业务代码根本没有跑，熔断器不会认为服务故障。限流用来保护自己，防止流量过大把服务压垮。”
>
> **演示二：熔断器完整生命周期（重点）**
> “限流是防止被压垮；熔断器解决下游故障雪崩。
>
> 如果下游大量报错，我们还不停调用它，会出现大量超时，请求堆积，故障扩散到整个系统。熔断器就会断开，停止调用故障服务，给下游留出修复时间。
>
> 我们给报表舱开启模拟故障100%报错，大量业务报错，样本收集够，失败比例超过阈值，变成红色熔断阻断。新的请求不再执行业务，直接拒绝。
>
> 熔断不会永远红色，等待冷却时间结束，进入黄色半开试探状态，放少量请求探测服务有没有修好。
>
> 如果试探依旧报错，说明服务还没修好，立刻再次熔断；我们取消模拟故障，下游恢复，试探全部成功，就恢复绿色正常。
>
> 大家注意：报表舱故障熔断，下单、营销舱完全不受影响。这就是水密隔舱故障隔离的价值。”
>
> **演示三：熔断+降级（营销舱）**
> “熔断之后有两种处理策略：直接返回错误，或者执行降级兜底。
>
> 营销舱开启模拟故障打到熔断Open，熔断器Open之后，不再调用真实业务，直接执行本地缓存兜底逻辑，返回可用结果，不对外抛错误。
>
> ⚠️演示注意：要在熔断冷却窗口期之内发送请求；时间过期熔断器悄悄进入半开，就不会触发降级。”
>
> **总结**
> “1.限流：保护自己，防止并发太大压垮服务；限流拒绝不等于业务失败。
>
> 2.熔断器：保护不被下游故障雪崩，只统计真正执行业务的成功失败。
>
> 3.降级：熔断打开之后的兜底策略，返回缓存数据，保障接口可用。
>
> 4.熔断器三个状态流转：正常 → 熔断阻断 → 半开试探。
>
> 5.不是一报错就熔断，要收集足够样本，失败占比达到阈值两个条件。
>
> 6.半开试探：成功就恢复，遇到失败立刻重新熔断。
>
> 7.网页故障注入可以快速模拟下游各种故障，不用修改代码。
>
> 注意这个是演示Demo，生产环境会更加复杂。”
## 10 生产环境注意事项与坑点
> ⚠重要：本项目是教学Demo，**禁止直接复制上线生产环境**。
1. **信号量限流**
   Demo使用`TryAcquire`非阻塞快速拒绝；生产根据业务选择非阻塞快速失败或者阻塞等待；优先推荐非阻塞防止goroutine堆积。
2. **熔断器实现差异**
- Demo是简单计数窗口；工业级组件（Sentinel、Resilience4j）使用滑动窗口统计；
- Demo半开放行全部流量；生产半开状态只允许极少量试探请求，防止把刚恢复的下游再次打垮；
- **错误分类过滤：用户参数错误、鉴权失败不计入熔断失败统计，只统计下游依赖故障类错误**。
- `Allow()`不是只读查询，具备状态变更副作用；UI界面状态快照不等于真实运行时状态。
3. **降级Fallback回调**
- 重建业务舱对象时，手动复制fallback回调函数，防止降级逻辑悄悄丢失；
- fallback内部需要增加超时、panic保护，防止兜底逻辑本身异常。
4. **指标与监控告警**
   Demo全部指标内存存储，重启丢失；生产需要上报Prometheus；熔断器状态切换、降级次数突增需要配置告警。
5. **舱粒度设计**
   最佳实践：**按外部依赖划分舱，不要按接口划分舱**；一个外部下游对应一个舱，粒度不能过细，也不能过粗。
6. **防护组合拳**
   完整的弹性防护顺序：**超时设置 > 舱壁隔离（信号量限流） > 熔断器 > 降级兜底返回数据**，多层防护缺一不可。
> 水密隔舱只是止损手段，**不会修复下游故障**；熔断发生之后，依旧需要人工排查修复故障服务，只是阻止故障扩散。
> 故障注入提醒：Demo网页故障注入仅用于教学演示；生产环境故障注入请使用 Chaos‑Mesh 等专业混沌工程平台，不要自己实现。
## 11 常见现象答疑
| 问题 | 解答 |
|------|------|
| **限流拒绝数字涨很多，熔断器依旧是绿色？** | 限流拒绝代表请求被拦在入口，业务没有执行，不会算作失败，熔断器不会触发。 |
| **已经看到大量业务报错，熔断器不会变红？** | 检查【统计样本请求数】，需要收集足够样本数量，才会计算失败占比；同时确认故障注入开关已经勾选。 |
| **半开状态一发起第二轮请求，立刻再次熔断？** | 辅助舱‑报表故障注入仍然开启，请求会报错；半开试探遇到报错，规则就是立刻切回熔断。 |
| **网页UI显示Open熔断阻断，但是新请求没有触发降级？** | 高频坑！`cb.Allow()`有状态变更副作用；熔断冷却时间到期，调用Allow会自动切换HalfOpen放行请求；**UI只是快照，不等于真实运行状态**。做演示调大熔断冷却时间，在窗口期之内发送请求。 |
| **单元测试降级用例PASS，网页演示降级不生效？** | 重建`BusinessCabin`结构体，忘记复制fallback回调函数，fallback被置nil；单元测试自己NewCabin并手动赋值fallback，不受影响；网页点击应用配置重建舱触发bug。 |
| **输入框修改数字，光标移开还没有点按钮，数值跳回旧值？** | 前端采用`document.activeElement`焦点保护方案；仅光标在输入框内编辑时保护输入；失去焦点后轮询会回填后端真实配置；操作习惯：修改参数后立刻点击提交按钮。 |
| **长时间反复跑演示，内存占用越来越高？** | Demo没有做内存优化，测试完成建议点击重置或者重启程序释放内存。 |
| **熔断器已经Open，窗口统计数字不再上涨？** | Open熔断状态下请求直接被熔断器拦截，不会执行业务，因此不会计入窗口成功/失败统计，属于正常现象。 |
| **执行请求时部分按钮变成灰色无法点击？** | 正在执行后台任务，开启了按钮置灰保护，防止重复点击；任务执行完毕按钮自动恢复可用；重置按钮始终可以点击。 |
| **点击【清空日志】之后，舱的状态、数字指标没有清零？** | **属于正常设计**。清空日志只清理日志输出，不触碰业务舱指标、熔断器状态；想要指标全部清零请使用【④重置所有舱状态】按钮。 |
## 12 单元测试、接口测试、集成测试执行与命令说明
> 测试文件命名：`go-go_test.go`，必须以`_test.go`结尾，和源代码放在同一个目录。
```bash
# 完整执行全部用例，-p 1串行执行；本项目存在全局共享舱、全局故障配置，必须串行，避免测试互相污染状态。
go test -v -p 1
# 带超时、覆盖率执行测试
go test -v -p 1 -cover -timeout 20s
# 只运行降级相关的测试用例（正则匹配TestMarketing开头）
go test -v -p 1 -run="TestMarketing"
# 生成覆盖率统计文件 cover.out
go test -v -p 1 -coverprofile=cover.out
# 浏览器可视化打开覆盖率报告
go tool cover -html=cover.out
```
### 关键测试说明
1. `TestMarketingCabinFallbackOpen`：单元模拟熔断器Open，验证降级兜底逻辑正常执行，不执行业务函数，降级计数+1。✅该用例PASS代表底层降级代码逻辑没问题。
2. `TestCabinReCreateLoseFallback`：复现网页重建舱丢失fallback回调的bug；PASS代表可以稳定复现该问题。
> 现象：单元测试全部PASS，网页演示降级失效，几乎都是**重建BusinessCabin忘记复制fallback回调**导致。
### 单元测试、接口测试、集成测试三者概念科普
| 测试类型     | 测试对象                                                           | 是否启动HTTP真实服务                      | 核心目标                                                                      |
| -------- | -------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------- |
| **单元测试** | 内部零件：`BusinessCabin`、`CircuitBreaker`、`SimpleErrGroup` 结构体方法逻辑 | ❌不需要                              | 验证底层零件逻辑正确性，拿到代码覆盖率；不碰http、不模拟网页操作。直接调用结构体方法，传入入参，断言返回error、状态、metrics指标。 |
| **接口测试** | HTTP单个API接口                                                    | ❌（使用`httptest`内存模拟http，不监听8080端口） | 校验每个接口的http状态码、入参校验、返回JSON报文结构正确性；一次只测一个接口。                               |
| **集成测试** | 完整业务链路，一串接口按顺序执行，模拟网页按钮全套操作                                    | ✅内存内完整运行整套业务逻辑                    | 验证多个模块组合在一起整体行为；模拟用户完整操作流程，验证端到端表现。                                       |
> 通俗大白话区分：
>
> 1. **单元测试**：单独测试一个个小零件，好比单独测试灯泡、开关好不好用。
> 2. **接口测试**：测试墙上插座通电是否正常，只关心单个接口本身。
> 3. **集成测试**：把整套电路全部接好，完整走一遍开灯‑关灯整套流程，看整套系统配合起来是否正常。
⚠重要注意点：
>
> - 单元测试要求**每次运行结果完全稳定可复现**；因此单元测试不要直接调用带有随机故障模拟的业务函数`bizMarketing/bizReport`，直接使用`cabin.Run()`传入确定回调。
> - 本项目集成测试会调用真实模拟业务函数，内部带有随机报错逻辑，**每次运行metrics、熔断器状态会有差异，允许非固定结果**，用来模拟真实世界的不确定性。
### 测试相关文件说明
- `go‑go.go`：业务源码，已将全局舱实例初始化从`main()`迁移到`init()`函数。
  > 原因：`go test`执行单元测试时**不会执行main()函数**；而`init()`无论`go run`还是`go test`都会自动执行，保证全局`cabinOrder/cabinMarketing/cabinReport`不会为nil，避免nil调用panic。
  >
  > 代价：全部测试用例共享同一套全局舱对象，测试之间会存在状态污染。
- `go‑go_test.go`：包含：单元测试用例 + httptest接口测试用例 + 集成测试用例。
### 编写与调试注意事项
1. 每一个测试case开头，必须调用重置：
```go
cabinOrder.Reset()
cabinMarketing.Reset()
cabinReport.Reset()
resetFaultConfig()
```
清除上一个测试用例残留的熔断器状态、metrics、故障配置，消除状态污染。
2. 不要直接在单元测试调用带有随机故障的演示业务函数`bizMarketing`，会产生偶现失败。
3. 集成测试内部会执行`sleep`等待熔断冷却时间，整套测试耗时会变长，属于预期现象。
4. 集成测试因为业务内部随机报错，不要硬编码写死熔断器状态断言；可以只校验字段存在、指标范围。
## 13 迭代：手动开关 + 可配置故障比例模拟业务故障（已完成）
#### 迭代效果
为营销舱、报表舱增加故障模拟配置；**下单舱作为核心业务，不支持故障模拟，始终业务正常**。
每个舱包含两组配置：
| 配置项 | 说明 |
|--------|------|
| **Enable 故障总开关** | 关闭则完全不模拟故障，业务 100% 成功；打开之后故障百分比才生效 |
| **FaultPercent 故障百分比（0‑100）** | 0%：开关打开也全部业务成功；50%：50% 概率返回业务异常；100%：每次请求必返回故障 |
后端新增接口：`POST /api/setFault`，接收舱名称、开关状态、故障百分比。`/api/status` 接口同步返回各舱故障配置，网页 UI 回显。
### 典型演示配置
| 场景 | 配置 |
|------|------|
| 快速触发熔断 | enable=true, faultPercent=100 |
| 模拟下游半不稳定 | enable=true, faultPercent=50 |
| 模拟故障恢复 | enable=true, faultPercent=0 |
### 迭代收益
**教学演示收益：**
- 无需修改源码重编译，网页界面即可切换故障模式
- 支持多种下游状态模拟：完全正常、部分不稳定、完全故障
- 可单独对某一个舱注入故障，其余舱正常，直观展示水密隔舱故障隔离
- 完整演示闭环：开启故障 → 报错累积 → 触发熔断 → 调为 0% 故障 → 半开试探 → 熔断器自动恢复
**自动化测试收益：**
- 彻底摆脱旧版硬编码随机报错；集成测试代码可直接设置故障配置
- 设置 100% 故障：每次请求必失败，得到完全稳定可复现的熔断触发用例
- 设置 0% 故障：模拟下游恢复，稳定验证半开恢复逻辑
- 设置 50% 故障：用于不稳定下游的探索测试
**代码解耦：** 业务函数本身只实现真实业务逻辑；故障注入作为演示层配置，与业务逻辑完全解耦。
> ⚠ **注意：** 故障配置保存在进程内存，程序重启配置丢失，仅用于教学 Demo。生产环境故障注入建议使用 Chaos‑Mesh 等专业混沌工程平台。
### 前端配套迭代
- 不再无条件 innerHTML 全量销毁重建 DOM；舱数量变化才全量重建
- 使用 document.activeElement 焦点判断，光标正在编辑输入框/复选框，跳过轮询回填旧值
- 页面说明文案同步更新，增加降级演示操作指引
### 额外前端UI迭代（已完成）
1. 舱卡片增加状态大背景色块：Closed浅绿 / HalfOpen浅黄 / Open浅红，提升投屏演示视觉辨识度。
2. 网页展示熔断器内部窗口统计：窗口成功计数、窗口失败计数、实时计算失败占比，直观讲解熔断触发双条件。
3. 增加运行状态按钮置灰保护：批量任务运行时禁用场景、流量操作按钮，避免重复点击造成metrics数据错乱；重置按钮始终可用。
> 以上改动**全部为前端JS实现，Go后端业务逻辑、单元测试完全不受影响。**

## 14. 迭代：日志面板‑清空日志 + 日志过滤（已完成）
#### 迭代效果
日志面板新增**清空日志按钮**与日志过滤下拉组件。
1. **清空日志按钮**：新增后端接口 `POST /api/clearLogs`；只清空内存事件日志数组，**不修改各个业务舱指标、熔断器状态**。适合多轮连续实验，不需要重置整个舱，仅清理日志面板的历史输出干扰。
> 对比：`POST /api/reset` 是完整重置，指标清零+熔断器切回Closed+清空日志。
2. **日志过滤下拉（前端实现）**
    - 全部日志：输出所有事件日志；
    - 仅熔断器事件：过滤只保留「熔断器切换」「半开试探」相关日志；
    - 仅降级事件：只保留「触发降级兜底」日志。
> 实现特点：过滤逻辑完全在前端JS完成，后端日志存储逻辑完全不变；基于日志文本关键词匹配。
> ⚠局限：如果后续修改`onEventLog`输出字符串文案，需要同步更新前端匹配关键词，否则过滤失效。

#### 教学演示收益
- 投屏演示场景，大量业务请求刷屏时，可以过滤掉噪音日志，只聚焦熔断状态切换或者降级事件；
- 可以保持熔断器Open状态不变，仅清空日志，继续观察后续事件，不用重新跑流量打熔断；
- 区分两个操作概念：“只清日志” vs “完整重置舱状态指标”。

#### 前端配套迭代
- 新增后端接口调用`/api/clearLogs`；
- UI增加按钮与下拉选择框；
- JS改造日志渲染函数`renderAllLogs`增加关键词过滤逻辑；
- 页面800ms定时轮询刷新，会保留用户当前选择的过滤模式。

## 15. 后续可迭代开发方向
按优先级划分：P0 优先实现，改动小，演示体验提升；P1 次要；P2 扩展；P3 探索性功能
> 区分迭代定位：
> - 教学演示向：面向课堂培训，提升原理理解，优先尽量仅改动前端，不改动后端核心业务逻辑
> - 贴近生产向：对齐 Resilience4j / Sentinel 工业组件行为，用于课后拓展对比学习
> - 不建议投入：UI美化、主题切换、花哨动画，对原理教学增益很低
### P0（优先做，改动小，演示体验提升，课堂收益极大）
- **✅已完成‑一键预设场景按钮：** 增加 3 个快捷按钮：场景 A‑限流压力测试、场景 B‑营销舱熔断+降级演示、场景 C‑报表舱故障隔离
- **✅已完成‑运行状态按钮置灰保护：** 批量请求执行时，按钮置灰不可重复点击，防止连续点击重复发任务造成 metrics 计数错乱
- **✅已完成‑日志面板增加【清空日志】按钮：** 一键清空，方便每一轮实验从头观察日志输出；可附加日志过滤，支持只查看熔断器事件、降级事件，屏蔽大量普通业务请求日志，避免演示时日志刷屏。
- **🔲待完成‑前端参数输入合法性校验：** 前端拦截非法输入：并发≥1，百分比 0‑100，冷却时间≥1
- **🔲待完成‑新增【仅重置指标，保留熔断器状态】按钮：** 现有重置接口会清零指标同时把熔断器强制切回 Closed；新增能力：只清空统计指标、清空日志，**保留当前熔断器 Closed/Open/HalfOpen 状态不变**。实验打到熔断状态后，可清零指标继续观察后续行为，不需要重新跑流量打熔断。
### P1（指标可视化，教学价值高，投屏演示友好）
- **✅已完成‑页面展示熔断器内部统计数据：** 网页暴露熔断器窗口内：窗口成功计数、窗口失败计数、实时计算当前错误占比
- **✅已完成‑强化状态色块视觉展示：** 每个舱卡片增加独立大色块，Closed 绿色 / HalfOpen 黄色 / Open 红色
- **🔲待完成‑熔断器冷却倒计时展示：** 当熔断器处于 Open 熔断状态，网页展示距离进入 HalfOpen 半开试探的剩余秒数，直观理解冷却窗口期概念。
- **🔲待完成‑失败类型明细与熔断统计过滤演示：** 区分模拟故障报错、下游超时、业务参数异常等不同失败类型；增加开关演示：部分错误类型不参与熔断器失败率统计，讲解生产环境熔断器错误过滤最佳实践。
### P2（增强功能，贴近真实生产行为，适合进阶教学）
> 本部分会少量修改后端业务逻辑，但保持原有水密隔舱、熔断器主体算法不变。
- **🔲待完成‑半开状态试探请求配额控制 ⭐⭐⭐⭐⭐：** 当前 Demo 简化实现：HalfOpen 半开放行全部流量；生产组件仅放行少量试探请求。增加配置项「半开最大试探请求数」，半开阶段只允许配额内请求通行，其余直接拒绝；模拟：试探失败立刻重新熔断，保护尚未恢复的下游，修复Demo与工业组件的关键差异点。
- **🔲待完成‑下游响应延迟可配置：** 每个舱支持配置正常耗时、故障耗时（ms），模拟慢 SQL、慢 RPC 超时故障。复现线上常见场景：慢调用耗尽信号量并发配额，导致正常请求被限流，不只模拟直接报错场景。
- **🔲待完成‑熔断器手动状态干预按钮：** 网页按钮可手动强制切换熔断器状态 Closed / Open / HalfOpen。无需跑批量流量，直接快速验证降级、半开逻辑，提升调试演示效率（仅教学演示，不代表生产实践）。
- **🔲待完成‑迷你条形占比进度条：** 每个舱指标展示极简横向迷你进度条，直观看各项指标占比
- **🔲待完成‑导出实验日志按钮：** 将当前内存内事件日志导出为 txt 文本，留存实验记录
- **🔲待完成‑各舱独立降级开关：** 下单舱、报表舱支持勾选开启/关闭降级，对比演示
### P3（深度贴近生产行为、工程化拓展，课后选学）
- **🔲待完成‑窗口模式切换：计数窗口 / 时间滑动窗口：** 当前为按请求数量的计数窗口；可切换为时间滑动窗口模式，对比两种窗口模型各自优缺点。
- **🔲待完成‑简易SVG时序事件图：** 不引入第三方图表库；X时间轴，Y业务舱；用不同颜色标记每个请求结果（成功、限流、报错、降级、熔断拒绝），直观观察事件流。
- **🔲待完成‑失败率迷你折线图：** 采集轮询历史数据，绘制失败占比变化曲线，可视化观察错误率上涨到阈值触发熔断的全过程。
- **🔲待完成‑动态新增业务隔离舱：** 网页可自定义新增水密业务舱，演示舱粒度过粗、过细带来的资源碎片化问题。
- **🔲待完成‑Websocket主动推送：** 替换现有800ms轮询；后端状态变更主动推送前端，消除页面刷新延迟，适配倒计时、时序图表。
- **🔲待完成‑指标快照JSON导出：** 导出全部舱配置、metrics、熔断器内部数据，用于实验作业复现。
- **🔲待完成‑多种流量发生器：** 可选固定并发、脉冲突增流量，自动产生请求，不用反复手动点击按钮。
- **🔲待完成‑模拟监控告警：** 配置告警阈值，失败率、降级次数突增网页弹出告警提示，讲解生产告警实践。
### 不建议投入开发的方向（教学收益低）
1. UI主题美化、各类花哨交互动画；
2. 完整Prometheus指标上报对接，本地教学Demo过重；
3. 登录鉴权、复杂业务流程等无关功能。
---
## 16. 关键代码说明
> 🎯建议：**先跑通上面实验，看懂网页现象之后，再来阅读本章节源码分析。**
### 16.1 熔断器状态枚举
```go
type State int32
const (
    StateClosed   State = 0 // 正常放行
    StateOpen     State = 1 // 熔断打开
    StateHalfOpen State = 2 // 半开试探
)
func (s State) String() string {
    switch s {
    case StateClosed:
        return "Closed"
    case StateOpen:
        return "Open"
    case StateHalfOpen:
        return "HalfOpen"
    default:
        return "Unknown"
    }
}
```
### 16.2 BusinessCabin 水密舱结构体（新增 fallback 降级回调）
每个舱拥有独立信号量、熔断器、统计指标、降级回调函数，实现隔离。
```go
type BusinessCabin struct {
    name           string
    maxConcurrency int64
    sem            *semaphore.Weighted
    cb             *CircuitBreaker
    fallback       func(ctx context.Context) error // 降级兜底函数，nil代表不启用降级
    metrics        CabinMetrics
    mu             sync.Mutex
}
```
### 16.3 业务执行入口 Run（熔断+降级逻辑）
执行顺序：**先判断熔断器 → 如果 Open 并且存在 fallback，执行降级兜底 → 再做信号量并发限流 → 执行业务 → 将成功失败结果反馈给熔断器**
> ⚠ **重要坑：c.cb.Allow() 不是只读查询函数，具备状态变更副作用！**
>
> Open 状态时间到期，Allow() 内部自动将状态切换为 HalfOpen，返回 true 放行请求。网页 UI 只是状态快照，UI 显示 Open，调用 Allow 之后状态可能已经变化。
```go
func (c *BusinessCabin) Run(ctx context.Context, fn func(ctx context.Context) error) error {
    c.mu.Lock()
    c.metrics.TotalTask += 1
    c.mu.Unlock()
    if !c.cb.Allow() {
        // 熔断器Open，优先执行降级兜底fallback
        if c.fallback != nil {
            c.mu.Lock()
            c.metrics.DegradeCount += 1
            c.mu.Unlock()
            onEventLog(fmt.Sprintf("[%s] 触发降级兜底，返回缓存活动数据", c.name))
            return c.fallback(ctx)
        }
        // 无降级，返回熔断拒绝错误
        c.mu.Lock()
        c.metrics.CbReject += 1
        c.mu.Unlock()
        return errors.New("cabin circuit breaker open: service reject")
    }
    // 信号量非阻塞获取，并发已满直接拒绝
    if !c.sem.TryAcquire(1) {
        c.mu.Lock()
        c.metrics.LimitReject += 1
        c.mu.Unlock()
        return errors.New("cabin semaphore full: concurrency limit reject")
    }
    defer c.sem.Release(1)
    var execErr error
    func() {
        ctxT, cancel := context.WithTimeout(ctx, 3*time.Second)
        defer cancel()
        execErr = fn(ctxT)
    }()
    if execErr != nil {
        c.mu.Lock()
        c.metrics.BusinessError += 1
        c.mu.Unlock()
    } else {
        c.mu.Lock()
        c.metrics.Success += 1
        c.mu.Unlock()
    }
    c.cb.OnResult(execErr != nil)
    return execErr
}
```
> **关键点：** 重建 BusinessCabin 结构体的时候，NewCabin 只会把 fallback 置 nil，必须手动赋值复制旧对象的 fallback 回调函数；单元测试可以稳定复现这个 bug，网页演示容易踩坑。
### 16.4 熔断器状态流转 OnResult
业务执行完成后回调，根据成功失败切换熔断器状态：
- **Closed 状态：** 收集样本，失败占比达到阈值切换为 Open 熔断
- **HalfOpen 半开状态：** 失败立刻切回 Open；成功切回 Closed 恢复正常
- **Open 状态：** 等待冷却时间到期，Allow() 内部自动切换 HalfOpen
### 16.5 故障注入相关结构体与逻辑
```go
type CabinFaultConfig struct {
    Enable       bool `json:"enable"`         // 故障总开关
    FaultPercent int  `json:"fault_percent"`  // 故障百分比 0‑100
}
// shouldProduceFault 判断本次请求是否模拟故障
func shouldProduceFault(cabinName string) bool {
    faultMu.Lock()
    defer faultMu.Unlock()
    cfg, ok := faultConfig[cabinName]
    if !ok || !cfg.Enable {
        return false
    }
    p := cfg.FaultPercent
    if p <= 0 {
        return false
    }
    if p >= 100 {
        return true
    }
    return rand.Intn(100) < p
}
```
业务函数 bizMarketing、bizReport 内部调用 shouldProduceFault() 实现动态故障模拟；业务本身逻辑与故障注入解耦。
---
# 限流 | 熔断 | 降级（精简核心版）
### 一句话区分
| 机制 | 一句话 |
|------|--------|
| **限流** | 防流量太大，管入口，挡住突发流量，保护自己不被打垮 |
| **熔断** | 防雪崩，管下游，下游大量报错就切断调用，不再访问故障依赖 |
| **降级** | 防自己扛不住，管自身，舍弃非核心逻辑，返回兜底结果保核心可用 |
### 1. 限流 RateLimit
- **作用：** 控制并发/QPS，超限直接快速拒绝
- **场景：** 大促、突发流量洪峰
- **常见算法：** 计数器、滑动窗口、漏桶、令牌桶
- **结果：** 返回请求繁忙，不执行业务逻辑
### 2. 熔断 CircuitBreaker
- **状态流转：** Closed(正常) → Open(熔断断开) → HalfOpen(半开试探) → Closed(恢复)
- **场景：** RPC/DB/第三方下游大量报错、超时
- **Closed：** 正常发起调用
- **Open：** 短路，不发起真实请求
- **HalfOpen：** 放少量请求试探下游是否修复
### 3. 降级 Degrade / Fallback
- **例子：** 商品页关掉推荐、返回缓存数据，优先保下单核心链路
- **本 Demo：** 营销舱熔断打开，不走真实业务，直接返回缓存兜底
### 熔断 vs 降级核心区别
| 机制 | 管什么 | 做什么 |
|------|--------|--------|
| **熔断** | 管下游 | 停止调用故障外部依赖 |
| **降级** | 管自己 | 执行兜底逻辑，保障对外返回可用结果 |
### 主流组件
| 组件 | 说明 |
|------|------|
| **Sentinel (阿里)** | 限流、熔断、降级一站式，国内微服务常用 |
| **Resilience4j** | Java 新一代，替代 Hystrix |
| **Hystrix** | 经典老组件，已停止维护 |
### 记忆口诀
> **限流挡流量，熔断断下游，降级保自己。**
---
## 调整说明
- 阅读顺序面向非开发人员：项目简介 → 部署运行 → 网页操作 → 做实验体验现象 → 参数调参 → 优缺点 → 演示话术 → 生产注意事项 → 答疑 → 单元测试 → 迭代规划 → 关键代码章节
- 增加提示说明：非开发人员优先跑通演示，最后再阅读源码剖析